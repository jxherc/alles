# Unknown syntax

The editor does not need to understand a construct to preserve it.

{#future-block owner=true}
@future-plugin(argument: "verbatim")
![[unrecognized.asset#fragment|alias]]

```query
where: syntax == future
keep: every byte
```
