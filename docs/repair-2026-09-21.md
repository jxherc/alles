# September 21 repair evidence

The prior task, `kokuen refine and testing`, published July's rebuild at `37242e6`.
This checkout had ten later local commits and stale mission notes. The repair preserves those commits
and the current three primary / nine specialist app structure. Product rework remains a
[separate proposal](plans/afterlife/product-direction-2026-09.md).

Published to `origin/dev-afterlife`: repairs `b1d4c2f`, formatter checkpoint `ece2cf9`, and the ten
earlier local commits. The remote matched the repair commit after the push; this publication note
is a subsequent documentation-only checkpoint.

## confirmed repairs

- Legacy text used the faint border color, making task empty states, metadata, and hints difficult
  to read. Foreground text now uses the muted text token; decorative tracks retain border colors.
- Light-theme inline code had matching text and background colors. Placeholder opacity, Files
  metadata, active Mail navigation, and light Server metric labels also failed measured contrast.
- The shared 44px minimum enlarged Settings and Andromeda switch tracks into circles or squares.
  Their targets now contain a separate 42 × 24px rounded track and centered 16px knob.
- Andromeda disabled its focused switch during saving, losing keyboard focus. It now exposes
  `aria-disabled` while the existing busy guard rejects repeat activation, preserving focus and rollback.
- Long Files location paths clipped their sidebar. They now wrap within the available width.
- Three JavaScript source assertions reflected old markup or line endings. The Finance browser
  fixture used a July date while verifying the current month. Both now match the actual behavior.
- Canonical design-linter discovery no longer depends on one developer's absolute checkout path.
- README and Afterlife indexes no longer describe implemented phases as unfinished or claim obsolete
  app counts and modes. Python and fenced Python formatting conforms to the installed formatter.

## fresh verification

All mutable app checks used marked temporary `ALLES_DATA` directories and separate localhost ports.
No normal owner database or vault was used.

| check | result |
|---|---|
| Python repository suite | 5,581 tests, passing with 7 environment-dependent skips |
| focused design, control-census, and lint tests after repair | passing |
| JavaScript suite | 581 passing |
| Ruff lint and format | passing |
| canonical KOKUEN audit | 0 errors, 195 explained warnings, 0 unexplained |
| default light/dark contrast at desktop/phone widths | 184 surfaces, 2,876 text observations, passing |
| literal resting controls | 92 surfaces, 1,683 visible controls, passing |
| finished-surface browser gate | passing, including long Files path containment |
| shared switches | pointer, Space, state, focus, reduced motion, desktop/phone passing |
| populated app journeys | Plan recovery; Library/Health; Files; Docs; Finance/Vault/Server; first-run setup/Passwords; PWA offline/reconnect passing |

Screenshots from the real application were inspected across primary and specialist screens, settings,
both themes, and desktop/phone layouts. The final switch and Files screenshots were inspected after
their fixes. The design-law review checked existing hierarchy, restrained color, readable text,
control geometry, focus, motion, and responsive containment against the affected screens.

The Python suite ran during the repair; subsequent Python production edits were formatter-only,
with unchanged syntax trees. Focused tests cover the changed test tooling. Some existing recovery
journeys inject failures to exercise retry paths; these support, but do not prove, live integrations.

## limits

Live model providers, mail accounts, bank connections, the owner's infrastructure, native macOS
packaging, and a running external Actual service were not certified. A clean isolated app and passing
tests cannot establish that the owner's configured services work. No DSH integration or product
rework is included. The pre-existing untracked `alles-full-review.md` remains an historical note.

Local run logs and screenshots are in the temporary directory named by `/tmp/alles-repair-current`.
They are not published because rendered server screens can contain machine-specific information.
