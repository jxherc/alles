# 2b — inline query blocks + saved views
Audit docs/evidence/2b/. Built parse_query_spec + query_block (grouping) + /query-block + fillQueryBlocks (renders ```query fences in preview); saved views (settings docs_saved_views, /views GET/POST/DELETE) + query-panel save/insert/load UI. Tests: test_vault_queryblock.py (14) + pw_queryblock_2b.py (8).
