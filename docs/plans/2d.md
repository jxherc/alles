# 2d — spatial canvas / whiteboard
Audit docs/evidence/2d/. .canvas JSON files in the vault (canvas_read/write/list + /canvas, /canvases endpoints); static/js/canvas.js overlay: pan, add text nodes, note-link nodes (open the note), drag, connect edges, delete, autosave; opened via the docs "canvas" button or ?canvas= deep-link. Tests: test_vault_canvas.py (8) + pw_canvas_2d.py (9).
