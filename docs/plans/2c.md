# 2c — Bases multi-view databases
Audit docs/evidence/2c/. base_view(folder→rows+column-union) + set_cell(edit→frontmatter) + base_rollup(count/sum over a relation field, case-insensitive, handles [[wikilinks]]); /base, /base-cell, /base-rollup; base panel with table/gallery/list views, inline editable cells, view switcher. Calendar/timeline view deferred (low-priority per ROADMAP). Tests: test_vault_base.py (11) + pw_base_2c.py (9).
