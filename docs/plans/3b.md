# 3b — live embeds + synced blocks
Audit docs/evidence/3b/. Live embeds: standalone allow-listed URL (YouTube/OpenStreetMap/tweet) or ![embed](url) renders a sandboxed iframe. Synced blocks: find_block resolves note#^id (trailing ^id marker, or own-line marker -> paragraph above); ![[note#^id]] mirrors that block, recomputed on each render so edits propagate. Tests: test_vault_block.py (8) + pw_embeds_3b.py (7).
