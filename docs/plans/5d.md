# 5d — mail: rules & AI

ROADMAP 5d: rules engine, vacation auto-responder, LLM smart compose/reply. The rule *matching* and
vacation *logic* are pure + tested; applying to the cache (markread/mute) is tested; IMAP move and
LLM generation stay best-effort/gated.

## 5d-1 — backend (unittest)
Files: `core/database.py`, `services/mail_rules.py` (new), `routes/mail.py`.
- `MailRule(mail_rules)`: id, match_field (from|subject), match_value, action
  (markread|mute|label|autoreply), action_arg, enabled, created_at.
- `mail_rules.apply_rules(msg, rules)` — matched actions for a message (pure).
- `mail_rules.run_on_cache(db, account_id, rules)` — apply markread/mute to cached rows; returns
  count.
- `mail_rules.vacation_reply_for(sender, vac, state, today)` — (reply|None, new_state),
  once-per-sender-per-day (pure).
- Routes: `GET/POST/DELETE /api/mail/rules`; `POST /api/mail/rules/run/{aid}`;
  `GET/POST /api/mail/vacation` (settings `mail_vacation`); `POST /api/mail/smart-reply`
  (gated on an enabled `ModelEndpoint` — disabled→`{enabled:false, suggestions:[]}`).

Tests (`tests/test_mail_rules.py`, ≥10):
1. `rule_crud` 2. `apply_rules_from_match` 3. `apply_rules_subject_match` 4. `apply_rules_no_match`
5. `apply_rules_disabled_skipped` 6. `run_marks_read` 7. `run_mutes` 8. `vacation_first_reply`
9. `vacation_skips_same_sender_day` 10. `vacation_disabled_none` 11. `smart_reply_disabled_no_endpoint`

## 5d-2 — frontend (Playwright)
Files: `static/js/mail.js`, `static/index.html`, `static/style.css`, stamps.
- A **rules** toolbar button → panel: rule list + add form (if from/subject contains X → action) +
  delete + "run now"; a **vacation responder** toggle + subject/body.
- A **suggest** button in compose that calls `/smart-reply` and shows suggestions (or a "configure a
  model" note when gated off).

Tests (`tests/pw_mail_rules_5d.py`, ≥8):
1. `rules_panel_opens` 2. `add_rule` 3. `rule_persists_reload` 4. `delete_rule`
5. `vacation_saves` 6. `run_rules` 7. `smart_suggest_graceful` 8. `zero_console_errors`

## Gate
ruff + format; node --check mail.js; regression 0 errors; full suite green.
