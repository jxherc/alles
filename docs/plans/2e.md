# 2e — panes, themes & offline
Audit docs/evidence/2e/. Per-vault CSS (_vault-theme.css, theme-css get/put, auto-injected <style>, editor panel); tabs strip with saved layout (localStorage docs-tabs, open/switch/close/persist); split pane (second open doc rendered side-by-side); offline doc editing rides the 1b SW outbox (queues PUT /file offline, replays on reconnect). Tests: test_vault_theme.py (6) + pw_panes_2e.py (10).
