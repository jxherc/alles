# 6a — files: recover & history

ROADMAP 6a: trash (1d ✓ already wired), version history (1e ✓ already wired), starred,
storage-quota view. Only **starred** + **quota** are new.

## 6a-1 — backend (unittest)
Files: `core/database.py`, `routes/files.py`.
- `FileTag.starred` (bool) column; `_tag_map`/`_decorate` carry `starred`; listings expose it.
- `PUT /api/files/star?path=` {starred} → set the star; `GET /api/files/starred` → starred files.
- `GET /api/files/quota` → `{used, total, free}` (walk `files_dir()` for used bytes + `shutil.disk_usage`).

Tests (`tests/test_files_recover.py`, ≥8):
1. `star_sets_flag` 2. `star_unsets` 3. `listing_carries_starred` 4. `starred_lists_only_starred`
5. `star_persists` 6. `quota_used_sums_files` 7. `quota_has_disk_totals` 8. `unstarred_not_listed`
9. `star_empty_path_400`

## 6a-2 — frontend (Playwright)
Files: `static/js/files.js`, `static/style.css`, stamps.
- A star toggle (☆/★) on file rows; a **starred** smart-folder / filter; a **storage-quota** bar.

Tests (`tests/pw_files_recover_6a.py`, ≥8):
1. `quota_bar_shows` 2. `star_toggle_on` 3. `star_persists_reload` 4. `starred_filter_lists`
5. `star_toggle_off` 6. `trash_still_works` 7. `versions_still_work` 8. `zero_console_errors`

## Gate
ruff + format; node --check files.js; regression 0 errors; full suite green.
