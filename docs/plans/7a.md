# 7a — gallery: organize & recover

ROADMAP 7a: captions/keywords/tags, hidden/locked album, trash (1d), favorites filter.
Trash + favorites already shipped — build captions/keywords + the hidden/locked album only.

## 7a-1 — backend (unittest)
Files: `core/database.py`, `routes/photos.py`.
- `Photo`: add `caption` (TEXT), `keywords` (TEXT csv), `hidden` (BOOLEAN). `_add_col` migrations.
- `_fmt` carries caption, keywords (list), hidden.
- `/list` excludes hidden. `PATCH /{pid}` accepts caption / keywords / hidden.
- `/search` haystack includes caption + keywords.
- `GET /hidden` gated by the vault (`_master_pw` dep) -> 403 locked, lists hidden when unlocked.

Tests (`tests/test_photos_organize.py`, >=8):
1. patch_sets_caption  2. patch_sets_keywords  3. keywords_normalized  4. list_carries_caption_keywords
5. search_matches_caption  6. search_matches_keyword  7. patch_hide_excludes_from_list
8. hidden_requires_unlock_403  9. hidden_lists_when_unlocked  10. favorites_filter_only_favs

## 7a-2 — frontend (Playwright)
Files: `static/js/photos.js`, `static/index.html`, `static/style.css`, stamps.
- Lightbox: caption input + keywords input + save button (PATCH); a "hide" button.
- A "hidden" album option that prompts for the master password (POST /api/vault/unlock) and lists
  hidden photos with the X-Vault-Token header.

Tests (`tests/pw_photos_organize_7a.py`, >=8):
1. caption_saves  2. keywords_save  3. caption_shows_on_reopen  4. hide_removes_from_grid
5. hidden_album_prompts_unlock  6. hidden_album_lists_after_unlock  7. favorites_filter_works
8. zero_console_errors

## Gate
ruff + format; node --check photos.js; regression 0 errors; full suite green.
