# 3c — publish + templates
Audit docs/evidence/3c/. Folder->site publishing: publish-folder mints doc shares for a folder; the /s/{token} doc viewer links [[wikilinks]] to other published docs (navigable site), unpublished links stay plain. Template buttons: %%button: Name%% renders a button that inserts that template. Base "+ row" creates a new note in the folder seeded with the column frontmatter. (single-doc publish already shipped in 2a/1a.) Tests: test_vault_publish.py (8) + pw_publish_3c.py (8).
