# 1c — Local text index

Audit: `docs/evidence/1c/`. `rag.py` is vault-only/in-memory/non-reusable. Build a reusable, persistent,
multi-kind index. Don't rebuild rag.py (it works).

## 1c-1 — textindex service + IndexChunk model  (unittest, ≥10, RED→GREEN)
- `core/database.py` `IndexChunk` (kind, ref, chunk_no, text, vec=JSON floats or "", created_at).
- `services/textindex.py`: `_chunk`, `index(db,kind,ref,text)` (delete old (kind,ref) → chunk → embed →
  insert; idempotent replace), `remove(db,kind,ref)`, `search(db,query,kind=None,k=5)` (cosine over
  stored vecs, `_jaccard` fallback when no embeddings; score>0; ranked), `reindex_kind(db,kind,items)`,
  `stats(db)`. Reuses `memory_store._embed/_cosine/_jaccard`.
- Tests (`tests/test_textindex.py`): index_creates_chunks, index_replaces_no_dupes, remove_deletes,
  search_ranks_relevant_first (fake embedder), search_kind_scoped, search_keyword_fallback (_embed→None),
  search_empty_index, search_no_match_returns_empty, multi_kind_isolation, persisted_in_db, stats_counts.

## 1c-2 — wire reindex-on-save + search API  (unittest, ≥8, RED→GREEN)
- `routes/vault_md.py`: after `write` → `textindex.index(db,'doc',path,content)`; after `delete` →
  `remove`; after `rename` → remove(old)+index(new). Best-effort (never breaks the save).
- `routes/textindex.py` (mount `/api/index` in `app.py`): `GET /search?q=&kind=&k=`,
  `POST /reindex` (rebuild 'doc' kind from the vault).
- Tests (`tests/test_textindex_api.py`, _embed patched→None for determinism): save_indexes_doc,
  edit_reindexes (old term gone / new found), delete_removes_from_index, rename_moves_index,
  api_search_returns_hits, api_search_kind_filter, api_reindex_rebuilds, api_search_empty_q.
