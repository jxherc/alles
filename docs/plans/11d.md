# 11d — Test hardening: per-module ≥8-test backlog + final regression sweep

ROADMAP 11d (M). Acceptance: `python -m unittest discover -s tests` green; broad + deep
Playwright sweeps pass with 0 real console errors.

## 11d-1 — backlog: every test module to ≥8 real cases
Audit (start of 11d) found **66** `tests/test_*.py` modules below the repo's ≥8-cases bar.
Bring each to ≥8 with GENUINE tests (real behavior / error paths / edge cases — no filler,
no stubbed/tautological asserts, no skips). Mirror each file's existing harness (ApiTest,
settings isolation, vault unlock token, etc.). Done in parallel batches; the gate is the full
suite staying green + zero new ruff errors.

Batches (6 modules each):
- A: docx_export, api_today, net_proxy, stt_local, usage, youtube
- B: agent_cwd, agent_intents, agent_skill_match, agent_sources, api_money, api_tokens
- C: api_uploads, api_usage, caldav_sync, money, agent_state, api_compare
- D: api_cookbook, api_documents, api_mcp, api_openai_compat, api_push, api_reminders
- E: api_search, api_setup, api_shared, api_timeline, api_webhooks, connections
- F: files_search, fx, imagegen, jobs, local_models, vcard
- G: api_gallery, api_settings, api_skills, api_vault, automations, cross_app_tools
- H: doc_revisions, files_store, ics, memory_store, photos_store, rag
- I: routing, subs_analytics, api_backup, api_projects, api_system, doc_import
- J: mail_cache, mail_threads, money_csv, notify, photo_sync, photos_search
- K: vault_theme, webpush, chat_helpers, doctor, llm, pwtools

## 11d-2 — final deep Playwright sweep
`tests/pw_final_11d.py`: not just load-each-host (pw_regression does that) but a *deep* sweep
that drives a real interaction in several apps (open a modal, type, navigate views) across the
ecosystem and asserts 0 real console errors. Plus `docs/plans/regression.md` documenting the
standing regression procedure.
