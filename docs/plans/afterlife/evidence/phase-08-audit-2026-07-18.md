# Phase 8 implementation and completion audit - 2026-07-18, finalized 2026-07-20

This is an implementation audit of the `dev-afterlife` worktree. A checked row means the current code
and a fresh focused test support it. It does not turn a focused pass into a full-delivery claim. The
final-gate rows are checked only where the recorded commands finished successfully.

## Scope and safety

- [x] The owner explicitly approved the exact five standalone specialist starters before the real UI
  changed.
- [x] Tests use throwaway `ALLES_DATA`; no owner database, vault, upload, provider secret, or managed
  service directory is a test target.
- [x] The existing Phase 0-7 dirty worktree is preserved.
- [x] No commit or push has been made.
- [x] Final Git index and no-commit/no-push state rechecked after every gate.
- [x] The owner removed autoreview from this delivery scope on 2026-07-20. This audit does not claim a
  clean autoreview.

## Pre-Stage-8 audit reconciliation

This is the checked/unchecked state immediately before the Phase 8 final gate. Phase 0-5 were already
audited requirement by requirement against the implementation; the twelve superseded rows remain
historical records and are not current work. Phase 6 has two complementary trackers, so their row
counts are shown separately rather than added together as unique requirements.

| Stage | Current checked | Unchecked / open | Superseded historical |
|---|---:|---:|---:|
| Phase 0 | 33 | 0 | 0 |
| Phase 1 | 32 | 0 | 1 |
| Phase 2 | 49 | 0 | 0 |
| Phase 3 | 36 | 0 | 2 |
| Phase 4 | 47 | 0 | 9 |
| Phase 5 | 29 | 0 | 0 |
| Phase 6 Docs/knowledge tracker | 36 | 0 | 0 |
| Phase 6 correction tracker | 123 | 0 | 0 |
| Phase 7 | 31 | 0 | 0 |

The Phase 0-5 totals come from the dated 238-row implementation audit: 226 current rows, zero open,
and 12 retained superseded rows. A fresh source count confirms 36 checked / 0 unchecked in the Phase 6
Docs tracker, 123 checked / 0 unchecked in the complementary correction tracker, and 31 checked / 0
unchecked in Phase 7.

The post-Phase-8 Phase 0 baseline is 113 mapped tables, 14 manifest root roles, 78 included routers,
814 method/path pairs with digest
`17dc13370e2b9bb3145f97b2ba237367391e21af35e6c129c7f687cc9688dc67`, and 23 jobs. This
supersedes the 107-table/795-route checkpoint quoted in older Phase 0-5 evidence without changing its
requirement status.

## 8A - specialist composition

- [x] The launcher publishes exactly Plan, Inbox, Library, Health, and Finance for the consolidated
  specialist set.
- [x] Plan composes Calendar, Tasks, and Reminders into a stable ordered agenda and keeps their full
  screens and APIs.
- [x] Inbox composes cached mail, selected-message, and contact context without merging records.
- [x] Library composes Books and saved reading; News requires the explicit Save to Library action.
- [x] Health composes habit rhythm and measurements without granting health context to Aide.
- [x] Finance composes Money, Subscriptions, reviewed imports, and visible managed-Actual state.
- [x] Old names, hosts, hashes, and requested subsections resolve through compatibility routing.
- [x] Real desktop/mobile, both-theme, keyboard, focus-return, reduced-motion, type-floor, 44-pixel
  target, rendered-contrast, overflow, visible-error, and clean-console checks pass.

Focused evidence:

- `node --test tests/js/specialist_groups_phase8.test.mjs`: 5 passed, 0 failed.
- `tests/pw_phase8_specialist_real.py` with an isolated server: passed at 1280 and 390 pixels in both
  themes, including keyboard cutover/rollback preview, focus return, compatibility routes, reduced
  motion, rendered contrast, visible errors, and clean browser console checks.

### Applicable KOKUEN and interface-law recheck

Every entry in the owner-provided interface law was reconsidered against the five approved
workbenches. Marketing-only hero, pricing, testimonial, logo-wall, footer, and landing-page patterns
are absent rather than marked as applicable product-UI requirements.

| Area | Checked result |
|---|---|
| Controls and semantics | No visible native select, checkbox, radio, context menu, browser confirm, or browser prompt is used. Group tabs and choices expose the appropriate tab/pressed state, keyboard behavior, focus ring, disabled state, and 44-pixel target. The universal rounded switch remains confined to the approved starter controls. |
| Content and motion | Product content is visible by default. Only inactive routed sections are hidden. No entrance-opacity gate, hover lift, button jump/scale, underline-fill animation, decorative float, parallax dependency, or motion-only state is present; reduced motion remains usable. |
| Layout and clipping | The workbenches use a deliberate three-column desktop task surface that stacks to one column on mobile. Text has stable gutters, variable copy cannot displace parallel actions, live content is not placed across clipped section seams, and desktop/mobile overflow checks pass. Empty space reflects empty owner data instead of a decorative filler panel. |
| Type, icons, and specificity | The existing local system type and KOKUEN hierarchy are reused with no new remote font, icon pack, fake brand, fake customer, fake app window, decorative quote, or invented product data. Helper text stays at or above 12 pixels in the rendered gate. |
| Color and material | The approved quiet tonal palette has no gradients, glows, glass, grain over content, default all-around shadows, icon tiles, candy accents, or background blobs. Active state uses weight and surface tone rather than an underline or dot. Cards do not lift. |
| Safety hierarchy | The Finance authority confirmation keeps its approved permission edge and distinct destructive action because it communicates a real ledger-authority boundary. It is functional safety hierarchy, not an ornamental accent-bar card or stock filled/outline CTA pair. |
| Rendered QA | Desktop/mobile, dark/light, keyboard, focus return, reduced motion, target size, type floor, overflow, error visibility, compatibility routing, and console checks pass. The new rendered check first found the light-theme rollback error at 2.37:1; raising the gate to WCAG AA then exposed legacy muted text at 3.25-3.45:1. The specialist surface now derives muted ink from 72 percent foreground color, the error uses 76 percent foreground plus 24 percent danger tone, and the complete rerun clears 4.5:1 for normal text and 3:1 for large text. |

## 8B - exact Finance foundation

- [x] Migration 0037 adds original/base currency, conversion evidence, stable import identity, import
  receipts, and subscription-payment evidence without replacing legacy columns.
- [x] Existing account values, transaction values, subscription prices, due dates, and payment history
  remain byte-for-byte equal across the migration fixture.
- [x] Old float-derived balances and the new decimal-text balances/aggregates match.
- [x] A second migration run leaves the entire foundation snapshot unchanged.
- [x] Migration 0038 adds and idempotently seeds the Actual authority/run/link state.

Focused evidence:

- `python3 -m unittest tests.test_finance_phase8_migration tests.test_actual_state_migration -v`:
  4 passed, 0 failed.

## 8C - reviewed imports only

- [x] Current CIBC/CMB connection evidence is recorded; no direct bank provider is advertised or
  enabled.
- [x] The exposed profiles are CIBC CSV, CMB CSV, and reviewed CMB notification input only.
- [x] Preview separates pending rows, exact duplicates, and changed-source conflicts.
- [x] Stable row occurrence identities preserve legitimate identical rows and make repeat imports no-op.
- [x] Receipts retain a source hash and per-row evidence without storing the whole source document.
- [x] Undo deletes only transactions owned by that receipt, in both legacy and canonical Actual modes.

Focused evidence:

- `python3 -m unittest tests.test_finance_imports_phase8 tests.test_actual_finance_canonical -v`:
  reviewed profiles, duplicate/conflict behavior, CNY mapping, notification ambiguity, receipt scope,
  Actual-only apply/undo, and no dual write passed.

## 8D - Actual core and authority

- [x] `@actual-app/api`, `@actual-app/cli`, and `@actual-app/sync-server` are exactly locked to 26.7.0.
- [x] The serialized bridge has bounded timeouts, structured results, secret redaction, and official
  API-only read/write/migration commands.
- [x] The loopback managed service owns install, private bootstrap authentication, health, start, stop,
  restart, update, backup, restore, and app rollback.
- [x] App rollback swaps the exact app and its manifest as one pair; failed update or rollback restores
  and restarts the prior pair.
- [x] Migration maps/reconciles accounts, opening balances, account kind/archive/display metadata,
  transactions, transfers, payees, categories, budgets, schedules, source-currency evidence, and
  subscription links.
- [x] Opening-balance transactions are not exposed as normal transactions or double-counted.
- [x] The cutover switches transaction/account/transfer writes to Actual only; legacy rows are unchanged
  and read-only.
- [x] Unsupported legacy-derived analytics return 409 after cutover instead of displaying frozen data.
- [x] Cutover and restore failure paths return authority/data to the unchanged Alles ledger.
- [x] Cold backup hashes every managed data file, restore verifies the inventory, and fresh-client
  read-back is required.
- [x] Refreshed official live gate passes both the synthetic fixture and a real migration-0037 upgraded
  ledger fixture after the final implementation changes.

Focused evidence:

- `python3 -m unittest tests.test_actual_migration tests.test_actual_finance_canonical
  tests.test_managed_actual tests.test_actual_bridge tests.test_finance_actual_api -v`: 32 passed,
  0 failed.
- `ALLES_RUN_ACTUAL_LIVE=1 python3 -m unittest tests.test_actual_live -v`: 5 passed in 55.790 seconds.
  The official 26.7.0 run covered install, private bootstrap, synthetic and migration-0037-upgraded
  parity, stable duplicate, transfer, backup hash, mutation, restore, and fresh-client read-back.

## Final delivery gates

- [x] Full Python discovery passes with exact run/pass/skip counts.
- [x] Full JavaScript discovery passes with exact counts.
- [x] Warning hygiene and strict datetime/SQLAlchemy warning regressions pass after Phase 8.
- [x] Refreshed official Actual synthetic/upgraded live gate passes.
- [x] Refreshed real Phase 8 browser gate and the full applicable KOKUEN/design-law review pass.
- [x] Repository-wide Ruff lint and format checks pass.
- [x] Diff integrity, forbidden native-choice controls, Git index, no-commit, and no-push checks pass.
- [x] This audit and the phase tracker are reconciled to the final command outputs.

Final evidence:

- `ALLES_DATA=<throwaway> python3 -W always::ResourceWarning -m unittest discover -s tests -v`:
  4,933 run, 4,928 passed, 5 skipped, 0 failed or errored in 558.544 seconds. The final log contains
  no `ResourceWarning`, unclosed database warning, deprecated `utcnow`, or deprecated
  `utcfromtimestamp` warning. FastAPI's installed TestClient still emits its separate upstream
  `httpx`/`httpx2` deprecation notice.
- Recovery boot probes initially exposed an intermittent 30-second cold-start deadline across
  changing historical schema prefixes. The bounded allowance is now 60 seconds; process crashes still
  fail immediately and staging still requires two successful boots. The complete three-test migration
  fixture matrix passed in 454.651 seconds, and the same all-prefix test passed inside final discovery.
- `node --test tests/js/*.test.mjs`: 419 passed, 0 failed in 4.172 seconds.
- `uvx ruff check .`: passed. `uvx ruff format --check .`: 891 files already formatted.
- `tests/pw_phase8_specialist_real.py` with isolated `ALLES_DATA` and port 8048: passed every desktop,
  mobile, dark, light, keyboard, focus, reduced-motion, 44-pixel target, type-floor, WCAG AA rendered
  contrast, overflow, compatibility-route, visible-error, and clean-console assertion.
- `tests/pw_phase8_specialist_starters.py`: all five approved starters passed desktop/mobile,
  keyboard, both themes, reduced motion, 200 percent zoom, custom controls, overflow, and clean
  console checks.
- `tests/pw_shell_pty_live.py`: desktop and mobile passed pending-folder materialization, xterm PTY
  command round-trip, interrupt delivery, overflow, and clean-console checks.
- The final live pass exposed npm 11's incompatibility with install-time `--no-bin-links`. The managed
  Actual candidate now permits package scripts, removes every npm `.bin` directory, rejects any
  remaining symlink, and passed 76 focused managed-service/bridge tests plus the official live gate.
- The PTY gate exposed an activation-order race that closed a newly materialized terminal. The task
  identity now updates before session activation; the live desktop/mobile gate passes with cache stamp
  277 so existing browsers receive the fix.

## Intentionally not claimed

- Direct CIBC or CMB provider sync is not shipped. This is the correct result of the proof gate, not a
  missing provider implementation.
- Legacy ledger deletion is not part of Phase 8. The rows must remain read-only for at least one stable
  release, then require a fresh backup and explicit owner confirmation before any later removal.
- Analytics that have not been rewritten against Actual are unavailable after canonical cutover. They
  are not silently served from frozen legacy transactions.
