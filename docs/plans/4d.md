# 4d — money: goals, reports, currency

ROADMAP 4d: savings/debt-payoff goals; custom-range reports + CSV export; multi-currency FX roll-up.

## 4d-1 — backend (unittest)
Files: `core/database.py`, `routes/money.py`, new `services/fx.py`.

- `Goal(money_goals)`: id, name, kind (savings|debt), target, current, monthly (contribution),
  created_at. Serialized with `progress` (savings: current/target; debt: paid-down fraction) and
  `eta_months` = ceil(remaining/monthly) when monthly>0.
- Goals CRUD: `GET/POST/PATCH/DELETE /api/money/goals`.
- `GET /api/money/report?start=YYYY-MM-DD&end=` → `{start, end, income, expense, net,
  by_category:[[cat,amt]]}` over the inclusive date range (splits honored, transfers excluded).
- `GET /api/money/report/export.csv?start=&end=` → CSV of txns in range (formula-injection safe).
- `services/fx.py`: static `RATES` (USD-relative) fallback + `get_rates()` (best-effort ECB refresh,
  never fails) + `convert(amount, frm, to, rates)`; symbol→code map ($→USD,€→EUR,£→GBP).
- `GET /api/money/networth-base?base=USD` → each account balance converted to `base`, summed.

Tests (`tests/test_money_goals.py` + `tests/test_fx.py`, ≥10 total):
1. `goal_crud` 2. `goal_savings_progress` 3. `goal_debt_progress` 4. `goal_eta_months`
5. `report_range_totals` 6. `report_by_category` 7. `report_excludes_out_of_range`
8. `report_csv_export` 9. `fx_convert_same` 10. `fx_convert_cross` 11. `fx_symbol_map`
12. `networth_base_rollup`

## 4d-2 — frontend (Playwright)
Files: `static/js/money.js`, `static/style.css`, version stamps.
- **Goals** card: add a goal (name/kind/target/current/monthly), progress bar + ETA, remove.
- **Reports**: a date-range report panel (start/end → totals + by-category + CSV export link).
- **Base currency**: a base-currency net-worth line/toggle (convert all accounts to a chosen base).

Tests (`tests/pw_goals_4d.py`, ≥8):
1. `goals_add_shows_progress` 2. `goal_eta_shows` 3. `goal_remove`
4. `report_runs_totals` 5. `report_export_link` 6. `base_currency_rollup`
7. `goals_persist_reload` 8. `zero_console_errors`

## Gate
ruff + format; node --check money.js; regression (seeded) 0 errors; full suite green.
