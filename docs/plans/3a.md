# 3a — rich blocks
Audit docs/evidence/3a/. Frontend-only: toggle callouts (> [!toggle]) render as <details>; ::: columns / +++ blocks render as flex columns; frontmatter cover (image) + icon (emoji) banner; @today/@tomorrow/@YYYY-MM-DD date mentions as chips; toolbar toggle + columns insert buttons. (callouts/highlight/divider/color already existed.) Tests: pw_richblocks_3a.py (10).
