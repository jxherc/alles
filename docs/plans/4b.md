# 4b — money: envelope budgeting (YNAB)

ROADMAP 4b: assigned-vs-spent envelopes, category rollover, per-category funding targets, Age of Money.

## 4b-1 — backend (unittest)
Files: `core/database.py`, `routes/money.py`.

- `BudgetAssignment(money_assignments)`: id, category, month (YYYY-MM), assigned (float). Upsert key
  = (category, month).
- `FundingTarget(money_targets)`: id, category (unique), amount (float), target_date (str).
- `_spending_by_cat(db, month)` helper — expense per category, honoring splits + excluding
  transfers (factor out of `summary`; summary reuses it).
- `GET /api/money/envelope?month=YYYY-MM` → `{month, to_be_budgeted, income, assigned_total,
  categories:[{category, assigned, spent, available, target:{amount,date,funded}|null}]}` where
  `available = Σ_{m≤month}(assigned − spent)` (rollover), `to_be_budgeted = income_this_month −
  assigned_this_month`.
- `PUT /api/money/envelope/assign {category, month, amount}` → upsert assignment.
- `PUT /api/money/envelope/target {category, amount, target_date}` → upsert (amount≤0 deletes).
- `GET /api/money/age-of-money` → FIFO-match income→expense; amount-weighted avg age (days) of the
  most recent spending; `{age, sample}`.

Tests (`tests/test_money_envelope.py`, ≥8):
1. `assign_upserts` 2. `assign_replaces_same_month` 3. `envelope_lists_categories`
4. `available_includes_assignment_minus_spent` 5. `available_rolls_over_prev_month`
6. `to_be_budgeted_is_income_minus_assigned` 7. `target_upsert_and_funded` 8. `target_delete_on_zero`
9. `age_of_money_simple_gap` 10. `age_of_money_none_when_no_income` 11. `envelope_spent_honors_splits`
12. `assign_unknown_month_defaults_current`

## 4b-2 — frontend (Playwright)
Files: `static/js/money.js`, `static/style.css`, version stamps.
- An **envelope budgeting** card: TBB banner; per-category rows with an assign input (PUT on
  change), an available figure (green/red), and a target progress bar when a target is set; an
  "Age of Money" stat card.

Tests (`tests/pw_envelope_4b.py`, ≥8):
1. `envelope_card_renders` 2. `tbb_banner_shows` 3. `assign_updates_available`
4. `available_reflects_spending` 5. `target_progress_shows` 6. `age_of_money_stat`
7. `rollover_visible_next_month` 8. `zero_console_errors`

## Gate
ruff + format clean; node --check money.js; regression (seeded) 0 errors; full suite green.
