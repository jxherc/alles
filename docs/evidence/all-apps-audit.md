# all-apps audit - 91 confirmed issues

### mail (7)
- [high/functional] services/mail_outbox.py:37-49
    WHAT: _default_send builds its own acct_dict with only imap/smtp/username/password/email/use_ssl — it drops auth_type, id, and the oauth_* token fields. For a Gmail "sign in with google" (OAuth) account, send_mail then takes the password branch and calls s.login(user, '') with an empty password, which fails auth. process_due catches the exception and leaves the row 'scheduled', retrying forever. Since the compose Send button routes through /send-undoable (a near-future ScheduledMail), EVERY message sent from an OAuth account silently never delivers and just piles up in the queue.
    FIX: Build the dict the same way the routes do: include auth_type, id, oauth_access_token, oauth_refresh_token, oauth_expires_at (or just reuse routes.mail._acct_dict(acct)). Then send_mail will take the XOAUTH2 path.
- [high/functional] routes/mail.py:448 and routes/mail.py:468
    WHAT: archive_message and the /move route pass acct (a MailAccount ORM object from _get) straight into mailsvc.move_message, but move_message -> _imap(acct) calls acct.get('imap_host'), which an ORM object doesn't have. It raises AttributeError, caught by the surrounding bare except, so the server-side IMAP move/archive never runs. The cache is still updated so the row vanishes from the inbox UI, making it look like it worked — but the mail is never actually moved/archived on the server (and reappears on the next full fetch from another device).
    FIX: Pass _acct_dict(acct) instead of acct, matching every other route (e.g. mailsvc.move_message(_acct_dict(acct), body.uid, 'Archive', body.folder)).
- [high/functional] static/js/mail.js:760 (with use at :838 and :850)
    WHAT: The schedule-send date input #mc-sched-date has class date-input but no data-type="date", so initDatePicker runs it in datetime mode and its .value returns 'YYYY-MM-DDTHH:MM'. The schedule handler then does send_at = `${date}T${time}` producing a malformed 'YYYY-MM-DDTHH:MMT09:00'. The scheduled mail's send_at is garbage — the displayed/stored time is wrong and lexical comparison in the outbox is unreliable, so scheduled send fires at the wrong time (or the user sees a broken timestamp in the scheduled strip).
    FIX: Add data-type="date" to the #mc-sched-date input so the picker returns a bare 'YYYY-MM-DD' that the `${date}T${time}` join expects.
- [med/functional] static/js/mail.js:441-446
    WHAT: The 30s background poll only skips when _filter is 'sent'. When the user is viewing flagged, vip, a category (cat:primary/social/promotions), drafts, or a label view, the poll fires loadInbox(false,true), which always renders the plain INBOX (applyFilter only handles 'unread'). So every 30 seconds the user's current view is silently clobbered and replaced with the full inbox.
    FIX: Bail out of the poll for any non-inbox/unread filter, e.g. `if (_filter === 'sent' || _filter === 'drafts' || _filter === 'flagged' || _filter === 'vip' || _filter.startsWith('cat:') || !_accounts.length) return;` (or only poll when _filter is 'inbox'/'unread').
- [med/functional] services/mail_oauth.py:17 (SCOPES) — manifesting in services/mail_outbox.py
    WHAT: Secondary to the outbox bug: even the live path aside, the scheduled/undo send for OAuth accounts cannot work until _default_send carries the oauth fields. Grouping note kept separate so the fix list is clear.
    FIX: Covered by fixing _default_send to include auth_type + oauth tokens; no scope change needed.
- [low/functional] static/js/mail.js:822 (serializeForm at :1258)
    WHAT: The compose 'discard this draft?' guard compares serializeForm(main), which only reads input/textarea/select. The message body is a contenteditable div (#mc-html, line 793), so typing a whole email body and clicking close discards it with no confirmation prompt — silent data loss of the body.
    FIX: Include the contenteditable body in serializeForm (e.g. also append `#mc-html`.innerHTML), or compare _composeBody()/_draftBody() against the initial snapshot.
- [low/dead] services/mail.py:735 (copy_message) and services/mail.py:748 (delete_message)
    WHAT: copy_message and delete_message are full IMAP wrappers that are never called from any route or the frontend (only referenced by tests/docs). They're dead in the product.
    FIX: Remove them, or wire delete to a real trash/delete route if a delete action is intended (the UI only archives/moves).

### money (5)
- [high/functional] routes/money.py:360-373 (update_txn)
    WHAT: PATCH /api/money/transactions/{id} does a raw setattr of body['amount'] with no finiteness check, unlike create_txn (line 335-336) which rejects non-finite amounts. Verified: PATCH {"amount": Infinity} returns 200 and stores inf in the DB. The read path's _fin() then silently coerces it to 0.0 (the comment at line 52-53 literally says 'reject them on input'), so the user's transaction amount silently becomes $0 — silent data corruption of a real money value. account_id is also set blindly (can re-point a txn at a non-existent account).
    FIX: In update_txn, before the loop, if 'amount' in body and not math.isfinite(float(body['amount'])): raise HTTPException(400, 'amount must be a finite number'). Optionally also validate account_id exists when present, mirroring create_txn.
- [med/functional] routes/money.py:899-922 (upsert_budget) + 1572-1600 (summary)
    WHAT: Tag budgets are a real backend feature (POST /budgets with tag, stored with category='' ) but summary() only ever computes spend-by-category and renders budgets via by_cat.get(b.category). For a tag budget b.category is '', so the summary row comes back as {'category': '', 'limit': 40, 'spent': 0.0} no matter how much was spent on tagged txns. Verified: spent $24 on coffee-tagged txns, summary shows the coffee tag budget as category '' / spent 0.0. So tag budgets always read as 0% used with a blank name.
    FIX: In summary(), compute spend-by-tag (sum |amount| of expense txns whose normalized tags contain b.tag) and, for budget rows where b.tag is set, report b.tag as the label and the tag spend; or, if tag budgets aren't actually wanted, drop the tag branch entirely (see dead-code note).
- [low/dead] static/js/money.js:300-314 (budgetsList) + 485-491 (_budgetForm); routes/money.py:893-922
    WHAT: The whole tag-budget path in the backend (BudgetBody.tag, the tag branch in upsert_budget, the tag field in list_budgets) is never reachable from the money UI — _budgetForm only has a category + amount input and addBudget posts {category, limit_amt} with no tag. So the tag-budget code is dead from the app's perspective and, when reached via API/agent, produces the broken summary row above.
    FIX: Either expose a tag option in _budgetForm/addBudget and finish the spend-by-tag computation in summary, or remove the tag branch (BudgetBody.tag, upsert_budget tag handling, the tag key in list_budgets/upsert response) so budgets are category-only.
- [low/perf] routes/money.py:1572-1618 (summary)
    WHAT: A single /summary request scans the full Transaction table three times plus a filtered scan plus a TxnSplit scan: _balances() scans all txns, then the month-filter loop (line 1581) scans the month, then _spending_by_cat() scans all txns again (+ all splits), then the trend loop (line 1605) scans all txns a third time. The month income/expense and the trend's current-month bucket are recomputed data the other passes already touch.
    FIX: Pull db.query(Transaction).all() once at the top of summary() and derive balances, this-month income/expense, by-category (via _distribute), and the 6-month trend from that single in-memory list (splits fetched once). Cuts ~3 full table scans per summary call to 1.
- [low/visual] static/js/money.js:162-166 (networthCard)
    WHAT: The net-worth line chart plots points at x = i*(W/(h.length-1)) — first point hard against the left edge, last against the right edge — but the month labels under it are flex spans each 100/h.length wide and centered, so each label sits at the center of its slot (~1/(2n) in from the edges). The points and their labels are therefore horizontally offset from each other. (The same label markup is correct for trendChart because those are bars centered in 100/n slots.)
    FIX: Either inset the polyline to match centered labels (x = (i+0.5)*W/h.length) or render the labels to match the edge-anchored points (e.g. first label left-aligned, last right-aligned, or position labels at the point x-coords) so labels line up under their data points.

### calendar (10)
- [high/functional] static/js/calendar.js:769 (openEditor), used by openEvent at 594-597
    WHAT: Clicking a specific occurrence of a recurring event opens the editor with the MASTER's start/end (event.start_dt), not the occurrence you clicked. The clicked date (occ) is only stored in _editOcc and never applied to the start field. Two user-facing breakages: (1) the editor shows the wrong date/time (e.g. you click the Jun-24 instance of a weekly event but see the original Apr-01 date), and (2) choosing 'This event' in saveEvent (line 988) creates the standalone exception copy at the master's date instead of the occurrence you edited, so the edited instance lands on the wrong day while the real clicked day still shows the unmodified recurrence.
    FIX: In openEditor, when occ is provided shift start (and end, preserving duration) onto the occurrence date before building the form: compute the occurrence start by combining occ's date with the master's time-of-day, and offset end_dt by the same delta. e.g. if (occ && event && !pf) { const ms=new Date(event.start_dt); const od=new Date(occ+'T00:00:00'); od.setHours(ms.getHours(),ms.getMinutes()); start = localISO(od); if (event.end_dt) { const dur=new Date(event.end_dt)-ms; endVal = localISO(new Date(od.getTime()+dur)); } }
- [high/functional] routes/calendar.py:185-201 (agenda) + static/js/calendar.js:422-441 (renderAgenda)
    WHAT: The agenda view does NOT expand recurrence. The /calendar/agenda endpoint queries CalendarEvent.start_dt directly, so a recurring event (daily standup, weekly meeting) appears at most once in the next-60-days agenda (only if its single stored start_dt falls in range) instead of on every occurrence. Recurring series that started in the past show up zero times. Agenda is a primary view button, so this makes it largely useless for anyone who uses repeats.
    FIX: Build the agenda by expanding events the same way month/week do. Reuse services.recur.expand over [today, today+days) for each event and group the occurrence datetimes by day, rather than filtering raw start_dt. Easiest: in renderAgenda, build the list client-side from expand(rangeStart, rangeEnd) (already implemented in calendar.js) instead of fetching /calendar/agenda, which also fixes the visibility/search gaps below.
- [med/functional] routes/calendar.py:132-143 (duplicate_event)
    WHAT: duplicate_event copies every column except id/created_at/caldav_uid, so it also copies subscription_id. Duplicating an event that came from an ICS subscription produces a clone still tagged with that subscription_id; the next feed refresh (refresh_subscription, line 399 deletes all events WHERE subscription_id == sub.id) silently deletes the user's duplicate. Data loss with no warning.
    FIX: Add 'subscription_id' to the excluded columns set on line 138: {"id", "created_at", "caldav_uid", "subscription_id"} so a duplicate becomes an independent local event.
- [med/functional] routes/calendar.py:486-494 (delete_subscription) vs 444-467 (create_subscription)
    WHAT: When a subscription is created without a calendar_id, create_subscription auto-creates a dedicated Calendar layer (lines 449-454). delete_subscription deletes the events and the subscription row but never deletes that auto-created Calendar. Removing a feed leaves an orphan empty calendar in the 'my calendars' sidebar forever, accumulating with every add/remove cycle.
    FIX: In delete_subscription, after deleting the sub, if its calendar_id points to a non-default Calendar that now has no remaining events and no other subscription references it, delete that Calendar too (or tag auto-created calendars with the sub id and clean them up here).
- [med/visual] static/js/calendar.js:566-573 (renderTimeGrid) + static/style.css:4053-4057 (.cal-tev)
    WHAT: Timed events in week/day view are all positioned with left:2px; right:2px (full column width). Overlapping events stack directly on top of each other, so when two events share a time the later-rendered one completely hides the earlier one — you can't see or click the covered event. No side-by-side collision layout exists.
    FIX: Group same-day timed occurrences into overlap clusters (events where start<otherEnd && otherStart<end), then within each cluster set width:calc((100%-4px)/N) and left:offset*share so concurrent events render in adjacent sub-columns instead of overlapping.
- [med/functional] static/js/calendar.js:489 (renderMonth byDay) and 558 (renderTimeGrid all-day filter)
    WHAT: Multi-day events only render on their start day. Month view buckets occurrences solely by o._date (start), and the week/day all-day row filters with sameDay(o._date, d). An all-day event with an end_dt several days later (a 3-day trip, a multi-day holiday from a feed) shows only on day 1 and is invisible on the remaining days.
    FIX: When an event spans multiple days (end_dt date > start_dt date), emit a chip/all-day bar for each day in the range. In renderMonth, loop from start date to end date and push the event into each day's byDay bucket; in the time-grid all-day filter, match days where d falls within [start_date, end_date].
- [low/functional] static/js/calendar.js:422-441 (renderAgenda)
    WHAT: Agenda ignores calendar-layer visibility and the sidebar search box. It fetches /calendar/agenda (all events, all calendars) and never runs visibleEvents()/_search, so hidden calendars still appear in agenda and typing in the search field filters month/week but not agenda — inconsistent with every other view.
    FIX: Build agenda from the same visibleEvents()/_search-filtered, recurrence-expanded list used by the other views instead of the unfiltered server endpoint (folds into the agenda-recurrence fix above).
- [low/visual] static/js/calendar.js:217-225 (_clockHtml) — no setInterval anywhere
    WHAT: The secondary-timezone world clock is rendered once in renderSidebar and never ticks; it shows a frozen time that only updates when the sidebar happens to re-render (navigation). A clock that displays a static, slowly-drifting-wrong time looks broken.
    FIX: Start a setInterval(60_000) when the sidebar mounts that updates #cal-worldclock .cal-wc-time (and clear it on re-render to avoid stacking intervals), or drop the clock if live updating isn't wanted.
- [low/visual] static/js/calendar.js:233 (renderFeeds)
    WHAT: Each subscription row renders a checkbox-style span `<span class="cal-cal-chk on">` identical in appearance to the calendar-visibility checkboxes in renderCalList, but it has no data-act and no click handler — it's purely decorative. It looks toggleable (same .cal-cal-chk styling) but clicking it does nothing, which reads as a broken control.
    FIX: Either wire it to toggle the feed's calendar-layer visibility (PATCH /api/calendars/{sub.calendar_id} {visible}) like the cal list does, or remove the .cal-cal-chk span from the feed row so it doesn't masquerade as an interactive toggle.
- [low/dead] routes/calendar.py:517 (_RSVP_STATUSES)
    WHAT: _RSVP_STATUSES = {"invited", "accepted", "declined", "tentative"} is defined in routes/calendar.py but never referenced there. The actual RSVP status validation lives in routes/shared.py, which defines its own copy. Dead module-level variable.
    FIX: Delete the unused _RSVP_STATUSES definition on line 517 of routes/calendar.py.

### tasks (4)
- [med/functional] services/task_nl.py:51
    WHAT: Quick-add priority parsing collapses both '!' and '!!' to priority=1. The UI and task editor treat priority as 0=none, 1=low, 2=med, 3=high (tasks.js:57 maps ['','low','med','high'][priority]; editor options at tasks.js:124). So a user typing 'call mom tomorrow !!' clearly meaning high gets a 'low' task, and there is no way to set med/high from quick-add at all. The parsed value is wrong vs. the rest of the app's scale.
    FIX: In parse_task, detect the count: use re.search(r"\s(!{1,2})(?=\s)", t) and set out['priority'] = 3 if two bangs else 1 (or 1/2/3 if you also support a single-tier). Keep the same strip regex. That makes '!' = high-ish and '!!' = high, matching the 1-3 scale the editor/render use.
- [med/functional] static/js/tasks.js:145
    WHAT: In the task editor, the quick-reschedule chips POST to /api/tasks/{id}/reschedule which commits the new due_date server-side immediately, but the handler only updates the #te-due input value and never calls loadTasks(). If the user clicks a reschedule chip and then 'cancel', the due date is already persisted in the DB while the visible task row still shows the old date until a manual refresh - the list is silently stale and cancel does not actually cancel the reschedule.
    FIX: Don't hit the server on chip click; instead just compute/set the #te-due input locally (or stage the value) and let the existing 'save' PATCH persist due_date. That way cancel truly cancels and the list updates through the normal save->loadTasks() path. If you keep the server call, call loadTasks() after it and treat the reschedule as committed (remove the cancel ambiguity).
- [med/functional] static/js/tasks.js:165
    WHAT: addTask() never clears the active search filter. _search and the #tasks-search input stay set, so after quick-adding a task while a search term is present, loadTasks() re-queries /api/tasks/search?q=... and the new task disappears from view unless it happens to match the query - looks like the add silently failed. The function already handles the done-tab case but not the search case.
    FIX: At the top of addTask (after a successful add), reset the search like the done-tab branch does: if (_search) { _search=''; const si=document.getElementById('tasks-search'); if(si) si.value=''; }  then loadTasks() shows the active list including the new task.
- [low/perf] routes/tasks.py:76
    WHAT: task_tree() runs two full-table scans of the tasks table: db.query(Task).all() at line 76 to build the children map, then db.query(Task).filter(done==False).all() at line 79 to get top-level tasks. The second query is redundant - the full row set is already in memory from the first.
    FIX: Fetch once: rows = db.query(Task).all(); build children from rows where parent_id; derive tops = _ordered([t for t in rows if not t.parent_id and not t.done]). Removes the second DB round-trip and keeps progress accurate.

### notes-docs (7)
- [med/functional] routes/vault_md.py:240
    WHAT: Renaming a FOLDER orphans the version history of every doc inside it. rename_file rewrites DocRevision rows with `filter_by(path=_norm_path(body.path))`, but `_norm_path('folder1')` becomes 'folder1.md' which matches no revision row. Meanwhile vault_md.rename() physically moves the child files (e.g. folder1/note.md -> folder2/note.md), so their revisions still point at the old folder1/note.md path. After a folder rename the History panel shows 'no earlier versions' for every doc that was inside it.
    FIX: After a folder rename, re-point child revisions: if the renamed path is a directory, run `db.query(DocRevision).filter(DocRevision.path.like(old_prefix + '/%')).update(...)` rewriting each path's old folder prefix to the new one (or iterate matching rows and replace the prefix), instead of the single `_norm_path` equality update which only fits file renames.
- [med/visual] static/js/vaultmd.js:1336
    WHAT: Inline query-block BAR chart value labels are hardcoded `fill="#e8e6e3"` (near-white). On the light theme (`[data-theme="light"]` has `--text:#111` on a bone-white background) those numbers above each bar are near-invisible / fail contrast. The matching category labels use `#6e6e6e` which is fine on both themes, so only the value labels are broken.
    FIX: Replace the hardcoded `fill="#e8e6e3"` on the bar value `<text>` with `fill="currentColor"` (or `var(--text)` via a CSS class on the svg) so the label follows the active theme like the rest of the chart.
- [med/dead] routes/vault_md.py:689-711
    WHAT: The spatial Canvas feature is unreachable from the UI. routes/vault_md.py exposes /canvases, GET /canvas, PUT /canvas and services/vault_md.py implements canvas_read/canvas_write/canvas_list, but static/js/vaultmd.js contains zero canvas code (grep for 'canvas' in the file returns nothing) and no other frontend module calls /api/vault-md/canvas*. The whole feature is exercised only by tests.
    FIX: Either wire a canvas entry point into the docs UI (tree shows only .md, so .canvas files are invisible today) or remove the dead routes + canvas_read/canvas_write/canvas_list service functions to drop maintenance surface.
- [low/functional] static/js/vaultmd.js:1293
    WHAT: A ```query``` fence with `chart: bar` (or pie/line) but no `group:` line silently renders no chart. The backend (services/vault_md.query_block) returns `chart` regardless, but _renderQueryResult only emits `_renderChart(...)` inside the `if (d.group && d.groups)` branch, so an ungrouped chart spec just shows the plain result list with the chart dropped.
    FIX: In _renderQueryResult, when `d.chart` is set but there are no groups, synthesize a single-series dataset (e.g. group by the displayed field or fall back to a count) before calling _renderChart, or surface a hint that `chart:` requires `group:`.
- [low/perf] routes/vault_md.py:477
    WHAT: diff_revision runs `db.get(DocRevision, a)` twice on the same line: `db.get(DocRevision, a).content if a and db.get(DocRevision, a) else ''`. Two identical primary-key lookups per diff request where one suffices.
    FIX: Fetch once: `ra = db.get(DocRevision, a) if a else None; old = (ra.content if ra else '') or ''`.
- [low/functional] routes/vault_md.py:256-257
    WHAT: Folder rename pollutes the reusable text index. rename_file always calls `_unindex_doc(_norm_path(body.path))` and `_reindex_doc(new_path, read(new_path)...)`. For a folder rename, `_norm_path('folder1')` -> 'folder1.md' (a key that was never indexed) and `new_path` is the bare folder name whose read() returns exists=False/content='' — so it inserts an empty index entry keyed by the folder path and never removes the real child-doc entries (still keyed under the old folder).
    FIX: Guard the index calls to only run when the renamed target is an .md file (the code already computes that for link rewriting via `new_path.lower().endswith(('.md','.markdown'))`); for folder renames, re-index the moved child docs instead.
- [low/functional] services/vault_md.py:613-622
    WHAT: Editing a list-valued frontmatter property through the Base table silently collapses it to a plain string. The table shows a list cell joined as 'alice, bob'; saving sends that string to /base-cell -> set_cell stores `props[key] = value` (a string), so 'people: [alice, bob]' becomes 'people: alice, bob, carol' as a scalar. Downstream consumers that treat the prop as a list (query filters with eq/contains on a list, base_rollup _link_name splitting) now see one comma-joined string.
    FIX: In set_cell, if the incoming value contains a comma (or the existing prop was a list), split on commas into a list before writing, mirroring the properties-panel saveProps logic; or render list cells distinctly so editing them doesn't flatten the list.

### photos-gallery (3)
- [med/functional] static/js/photos.js:364
    WHAT: uploadPhotos sends the current dropdown value as album_id whenever _album is truthy. When the user is viewing a virtual filter (__fav__, __hidden__, __map__, __memories__), the uploaded photo is saved with a junk album_id like "__fav__" or "__hidden__" (upload route stores `album_id=album_id or None`). The photo doesn't actually get favorited/hidden, it just gets tagged with a non-existent album id, so it's permanently associated with a bogus album that no /albums row matches. This is a silent data-integrity bug.
    FIX: Only attach a real album id: `if (_album && !_album.startsWith('__')) fd.append('album_id', _album);`
- [low/functional] routes/photos.py:339-353
    WHAT: sync_macos creates a temp dir via tempfile.mkdtemp(prefix='alles-photos-'), imports from it, but never deletes it. On a real Mac run each call leaks a full copy of the exported Photos library into the system temp dir, which can be many GB and accumulates every sync.
    FIX: Wrap in try/finally and shutil.rmtree(dest, ignore_errors=True) after sync_folder returns (the originals are already copied into the photos library by import_image, so the temp export is safe to remove).
- [low/functional] static/js/photos.js:271-276
    WHAT: The search input filters the view but switching the album dropdown (line 247) calls loadPhotos() without clearing the search box. The stale query text stays visible in #photos-search while the grid shows the freshly-selected album's full contents, so the UI claims to be filtered by a term it is no longer applying.
    FIX: In the album change handler, clear the search field before reloading: `const s=$('photos-search'); if(s) s.value='';` then loadPhotos().

### contacts (9)
- [high/functional] routes/contacts.py:286-298
    WHAT: delete_contact only does db.delete(c). It never deletes the contact's ContactField, ContactGroupMember, or ContactLink rows (these tables use plain String columns with no ForeignKey/cascade). Every deleted contact leaves orphaned field rows, group memberships, and graph edges in the DB forever. Reverse ContactLink rows from other contacts now point to a non-existent id. The on-disk avatar file is also never removed.
    FIX: Before db.delete(c): db.query(ContactField).filter(ContactField.contact_id==cid).delete(); db.query(ContactGroupMember).filter(ContactGroupMember.contact_id==cid).delete(); db.query(ContactLink).filter((ContactLink.from_id==cid)|(ContactLink.to_id==cid)).delete(synchronize_session=False); and unlink the avatar file via _avatar_dir() like delete_avatar does.
- [med/functional] services/carddav_sync.py:177
    WHAT: The CardDAV pull loop copies only name/email/phone/company/title/address/website onto the local row. It omits `birthday` and `notes`, even though parse_vcards extracts both (BDAY/NOTE) and build_vcard pushes both. So a contact synced from iCloud/Google with a birthday or note silently loses that data locally, and it never shows up in the birthdays panel. Round-trip data loss on every pull.
    FIX: Add "birthday" and "notes" to the tuple on line 177: `for k in ("name","email","phone","company","title","address","website","birthday","notes"):`
- [med/functional] routes/contacts.py:564-593
    WHAT: merge_contacts moves ContactField and ContactGroupMember rows from the merged-away contact to the primary, but does NOT move/clean ContactLink rows. Relationship edges that pointed to the deleted `other` contact become dangling (point to a now-deleted id). It also never re-indexes: the primary's merged tags/notes aren't pushed to personal_index, and the deleted `other` is never removed from the search index, so the merged-away contact lingers in global search pointing at a deleted id.
    FIX: Add a ContactLink re-point (UPDATE from_id/to_id = p.id where = o.id, then de-dup self/duplicate edges), call personal_index.remove_record(db,'contact',o.id) and index_record(db,'contact',p) before returning.
- [med/functional] routes/contacts.py:173-195
    WHAT: import_contacts (bulk vCard import) creates Contact rows but never calls personal_index.index_record, unlike create_contact. The same is true for the CardDAV pull in services/carddav_sync.py:174-182. So bulk-imported and synced contacts are invisible to the app's global personal search until each one is individually edited (which triggers patch_contact's index call).
    FIX: After db.commit() in import_contacts, loop the created rows and call personal_index.index_record(db,'contact',c) (wrap in try/except like the other call sites). Do the same in carddav_sync.sync for newly pulled rows.
- [med/perf] static/js/contacts.js:88
    WHAT: _avatarHtml builds the avatar src with `?ts=${Date.now()}`, a fresh cache-buster on every single render. loadContacts re-renders the whole list on every search keystroke and every favorite toggle, so every contact's avatar image is re-downloaded from /api/contacts/{id}/avatar on each keystroke. Defeats browser caching entirely.
    FIX: Use a stable cache key tied to the contact instead of Date.now(), e.g. `?v=${encodeURIComponent(c.updated_at)}` so the URL only changes when the contact (and thus its avatar) actually changes. Update get_avatar to send a normal Cache-Control header.
- [low/perf] static/js/contacts.js:220-221
    WHAT: showGroups fires one /api/contacts/groups/{id}/members fetch per group sequentially inside a for-await loop (N+1 round-trips, serialized). With several groups the panel paints slowly, one group at a time.
    FIX: Fire the member fetches in parallel with Promise.all, or add a member_count to the list_groups payload so no per-group fetch is needed just to show the count.
- [low/perf] static/js/contacts.js:138
    WHAT: openContact fetches the ENTIRE /api/contacts list and .find()s one contact by id. This runs on every open and again after every field add/delete and after set-as-me. There is no single-contact GET endpoint, so opening one card pulls every contact (each with its fields sub-query) repeatedly.
    FIX: Add `@router.get('/contacts/{cid}')` returning `_fmt(db.get(Contact,cid), db)` (404 if missing) and have openContact fetch `/api/contacts/${id}` instead of the whole list.
- [low/functional] static/js/contacts.js:252
    WHAT: The duplicate-merge button hardcodes data-merge=cl.contacts[0].id and data-other=cl.contacts[1].id. A cluster with 3+ duplicates only merges the first two; the 3rd is left behind. The button label 'merge these' implies the whole cluster gets merged, so the user thinks they're done when they aren't.
    FIX: Either loop-merge contacts[1..] into contacts[0] (one merge call per extra member) in the click handler, or relabel to make it clear only a pair merges and keep the cluster visible until reduced to one.
- [low/dead] routes/contacts.py:28-52
    WHAT: The contact relationship-graph routes (GET /contacts/{cid}/related, POST /contacts/{cid}/links, DELETE /contacts/{cid}/links/{other_id}) and services/contacts_graph.related_for_invite have zero frontend usage — contacts.js never references /related, /links, or relationships, and related_for_invite is only called from tests. The whole ContactLink feature is reachable by API but has no UI, and merge/delete don't maintain its rows (see above), so any edges that did exist would rot.
    FIX: Either wire a relationships UI into the contact detail view (openContact) so the routes are reachable, or drop the unused routes + related_for_invite if the graph feature isn't shipping.

### vault (5)
- [high/functional] static/js/vault.js:198-213
    WHAT: _switchVault never checks j.requires_2fa. If the target vault has 2FA enabled, POST /api/vault/unlock returns {requires_2fa:true, methods:[...]} with NO token (r.ok is still true). The code then sets _token=j.token (undefined) and _vaultId=j.vault_id (undefined), so the next _loadEntries() sends no/garbage token, the server 403s, the catch in _loadEntries flips _unlocked=false and the whole view collapses to the locked screen. A 2FA-protected vault becomes completely unreachable through the vault switcher.
    FIX: After `const j = await r.json();` add: `if (j.requires_2fa) { await _do2fa(pw, {...j, vault_id: id}); return; }` and have _do2fa use the passed vault_id instead of the module _vaultId so the TOTP unlock targets the right vault. Also restore the switcher selection (_renderSwitcher()) if 2FA is cancelled.
- [high/functional] static/js/vault.js:1099-1115 (and dead route routes/vault.py:1093 twofa_unlock)
    WHAT: Security-key-only 2FA unlock is not wired up. When a vault has a hardware/passkey second factor but NO TOTP, /vault/unlock returns methods:['passkey'] and _do2fa just shows the toast 'this vault needs its security key — use "unlock with biometric"'. But the biometric button unlocks via the role="" biometric credential, not the role="2fa" security key, and the implemented endpoint POST /api/vault/unlock/2fa (twofa_unlock) is never called anywhere in the frontend. Result: a user who enrolls only a security key as 2FA is permanently locked out of that vault, and twofa_unlock is dead server code.
    FIX: Implement the passkey branch in _do2fa: when methods includes 'passkey', call navigator.credentials.get with j.challenge/j.credentials, then POST authenticator_data/client_data_json/signature/credential_id/password to /api/vault/unlock/2fa and set _token from the response. Or, if you don't want to support it, remove the twofa_unlock route and the passkey method from the unlock response so users can't enroll a factor they can't use.
- [med/functional] routes/vault.py:566-573 (delete_entry) and 727-746 (share_entry)
    WHAT: Deleting an entry only does db.delete(e). Its VaultAttachment rows, the on-disk {att.id}.enc files, and any VaultShare row for that entry are left behind. The orphaned VaultShare keeps the /sv/{token} share link live and decryptable after the entry is deleted (you think you deleted the secret but the public share URL still serves it), and the .enc attachment blobs leak on disk forever.
    FIX: In delete_entry, before db.delete(e): delete VaultShare rows for entry_id, query its VaultAttachment rows, unlink each _attach_dir()/f"{a.id}.enc", and delete those attachment rows. Same cleanup belongs in delete_vault.
- [med/functional] routes/vault.py:315-325 (delete_vault)
    WHAT: delete_vault removes the Vault and its VaultEntry rows, but leaves VaultAttachment rows + their .enc files, VaultShare rows, and WebAuthnCredential rows (both biometric and role=2fa) for that vault. Deleting a non-default vault orphans all of those: attachment ciphertext stays on disk, share links stay live, and stale credentials accumulate.
    FIX: Collect the vault's entry ids, delete their attachments (rows + .enc files) and shares, delete WebAuthnCredential rows where vault_id==vid, then delete the entries and the vault.
- [med/functional] routes/vault.py:514-534 (patch_entry), 537 (reveal), 587 (totp), 657/680/688 (attachments)
    WHAT: patch_entry (and reveal/totp/attachment routes) resolve only the master password via _master_pw and then db.get(VaultEntry, entry_id) with no check that e.vault_id matches the caller's unlocked vault_id. patch_entry is the dangerous one: it re-encrypts e.value_encrypted with the CURRENTLY unlocked vault's password while leaving e.vault_id pointing at the other vault. If an entry id from vault B is patched while vault A is unlocked, that entry is now ciphertext under A's key but still lives in vault B — decrypt will permanently fail when B is unlocked (silent data loss).
    FIX: In these handlers, after fetching the entry compare e.vault_id against the unlocked vault id (use ctx=Depends(_ctx) to get (pw, vid)) and raise HTTPException(404) when they differ — matching how passkey_sign already guards with `e.vault_id != vid`.

### journal (5)
- [high/functional] static/js/journal.js:410
    WHAT: The "reflect" button is completely broken whenever a journal passcode is set. reflect() calls fetch('/api/journal/'+_day+'/reflect', { method:'POST' }) with NO auth headers, but the /journal/{day}/reflect route depends on _require_unlock. Verified live: with a passcode set and a valid token held, the tokenless reflect call returns 403 (reflect_no_token=403) while the same call WITH the token reaches the model layer (reflect_with_token=400 'no model'). So the user always sees 'reflect failed' even though they're unlocked. Worse: because it's a raw fetch (not jget/jput), the 403 doesn't trigger showLock(), so the lock state isn't even surfaced.
    FIX: Send the journal token: const r = await fetch('/api/journal/'+_day+'/reflect', { method:'POST', headers: _authHeaders() }); and treat a 403 the same as jget/jput (call _setToken('') + showLock('unlock')).
- [med/functional] static/js/journal.js:321-337 (load) vs 106-115
    WHAT: Autosave race causes data loss / cross-day writes on quick navigation. The text 'input' handler arms _saveTimer = setTimeout(save,1200). load() (called by prev/next/today/heatmap/recent/search clicks) changes _day and overwrites the textarea value but never clears _saveTimer. If you type and then navigate to another day within ~1.2s, the pending autosave fires after the swap, reads the now-replaced textarea and writes it against the NEW _day — silently discarding what you typed for the original day and doing a stale write to the new one.
    FIX: At the top of load() add clearTimeout(_saveTimer); (and ideally flush a pending save for the outgoing day first, or capture _day at timer-arm time and save against that captured day).
- [med/perf] static/js/journal.js:357-364,387-401 + routes/journal.py:140-164
    WHAT: Two redundant full-table fetches on every load() and every save(). loadStats() GETs /api/journal and uses only d.stats (discards entries); loadRecent() GETs /api/journal?limit=30 and uses only d.entries (discards stats) — the single endpoint already returns both. On top of that the list_entries route never applies a SQL LIMIT: it does rows = q.order_by(...).all() (loads every entry incl. full content column) then slices rows[:limit] in Python, AND runs a second full scan db.query(JournalEntry.date).all() plus a count(). So each save triggers loadStats+loadRecent = 2 round-trips, each loading the entire journal table into memory, just to render 30 rows + a few counters.
    FIX: Call /api/journal once per render and use both d.stats and d.entries from the same response (drop the separate loadStats fetch, or have loadRecent feed loadStats). In the route, push the limit into SQL: q.order_by(JournalEntry.date.desc()).limit(limit).all() for the entries, keeping the lightweight date-only query for stats.
- [low/dead] static/js/journal.js:323
    WHAT: Dead/no-op ternary: textContent = pretty(_day) + (_day === todayISO() ? '' : '') — both branches produce '', so the conditional does nothing (looks like a half-finished 'today' badge). It just adds confusing dead code.
    FIX: Drop the ternary: document.getElementById('jrnl-date').textContent = pretty(_day);  (or, if a 'today' marker was intended, make the true branch actually emit something like ' · today').
- [low/visual] static/js/journal.js:381
    WHAT: In the 'on this day' list the row template always appends a literal '…' after esc((e.content||'').slice(0,90)), regardless of length. Short entries and entries with empty content render a dangling '…' (e.g. '2024 🙂 …' or 'short note…') implying truncated text that isn't there. Inconsistent with the 'recent' list which relies on CSS ellipsis.
    FIX: Only append the ellipsis when truncated: const c=(e.content||''); const snip=esc(c.slice(0,90))+(c.length>90?'…':'');  and use snip in the template.

### habits-health (8)
- [med/functional] static/js/health.js:98-112 (and routes/health.py:92-106)
    WHAT: The health add form never collects a label, but the overview groups cards by `kind`. So every entry logged under the "custom" kind collapses into a single "custom" card and a single mixed series. Verified: logging blood pressure (120, 130 mmHg) and steps (5000) as custom produces one card whose chart plots 5000 next to 120 — a meaningless line. The backend fully supports per-label custom metrics (HealthEntry.label, _fmt returns it, card label uses k.label) but the UI gives no way to set it.
    FIX: Add a label text input to _addForm that only shows (or is used) when kind === 'custom', send it as `label` in _create's POST body, and in routes/health.py group the overview by (kind, label) instead of kind alone so distinct custom metrics get separate cards.
- [med/functional] routes/habits.py:148-153
    WHAT: update_habit (PATCH) writes `target` straight through with no clamp, unlike create_habit which does max(1, body.target). Verified a PATCH with target=0 or target=-5 persists, and the card meta then renders "3/0 this week" or "5/-5 this week". The edit form (habits.js _editCard) also has no min validation, so a user typing 0 in the weekly target field saves a broken habit.
    FIX: Clamp target on patch the same way create does: in the field loop (or right after), if f == 'target' set v = max(1, int(v)). Optionally also coerce on the client in habits.js _wire save (clamp t to >=1).
- [med/functional] routes/health.py:88-106
    WHAT: The overview's card list is built from `all_rows` (every entry ever, unfiltered), while each card's `series` uses the range-filtered `rows`. So a metric whose entries are ALL outside the selected range still gets a full card. Verified: a weight entry 400 days old renders a weight card showing latest=80kg with series_len=0 ("need 2+ entries to chart", "0 in range") even when the user picked the 1y range — the range chip is effectively ignored for which cards appear, which is confusing.
    FIX: Build the kinds/cards list from the range-filtered `rows` (only show a card if the metric has >=1 entry in range), while still allowing `latest` to come from all_rows so the headline value stays current. i.e. iterate `rows` for `seen`/kinds, keep latest_per_kind on all_rows.
- [med/perf] routes/habits.py:100-101
    WHAT: N+1 query in the habits overview: one query lists the habits, then _dates_for runs a separate HabitLog SELECT per habit. With H habits that's H+1 round trips on every overview load (called on init and on every tab visibilitychange).
    FIX: Fetch all logs for the active habits in one query (e.g. db.query(HabitLog.habit_id, HabitLog.date).filter(HabitLog.habit_id.in_([h.id for h in habits])).all()) and bucket dates into a dict by habit_id, then pass the prebuilt set into _fmt — turning H+1 queries into 2.
- [low/dead] static/js/habits.js:65 (color usage) vs _addForm:104-118 and _editCard:87-102
    WHAT: _card reads `h.color` to set --habit-accent, but no form anywhere collects a color: _addForm and _editCard only have name/icon/cadence/target inputs. So every habit always has color="" and renders with the default accent. The whole per-habit color feature — HabitBody.color, create/patch color handling, _fmt's color field, and the --habit-accent CSS plumbing — is unreachable dead surface from the user's side.
    FIX: Either add a small color picker/swatch input (data-f="color") to _addForm and _editCard and send it in _create/save, or, if per-habit color isn't wanted, drop the color column handling from the body models and the --habit-accent indirection and just use var(--accent).
- [low/perf] routes/health.py:86,88
    WHAT: The overview runs two full reads: `rows` (range-filtered) plus `all_rows` = every HealthEntry ever, loaded into memory only to dedupe the kind list and find the latest-per-kind. For a long-running log this scan grows unbounded each call even though only the most-recent row per kind is needed.
    FIX: Replace the all_rows scan with a grouped/max query for latest per kind (e.g. a subquery selecting max(date) per kind, or func.max on id), and derive the kind list from that — avoids materializing the entire table on every overview load.
- [low/functional] services/imports.py:61 + routes/health.py:145
    WHAT: CSV import inserts whatever `kind` string the file contains (defaulting to 'custom') with no validation, while create_entry (routes/health.py:164-165) rejects any kind not in KINDS. So import can create entries with kinds like 'steps' or 'bp' that the manual add path forbids, leaving cards with no default unit and an inconsistent data model.
    FIX: In import_health, coerce any kind not in KINDS to 'custom' (and stash the original name as label), or validate/skip out-of-set kinds, so imported data matches what create_entry allows.
- [low/dead] routes/habits.py:85-93 and routes/health.py:57-68
    WHAT: The GET /api/habits/{hid}/risk and GET /api/health/{kind}/anomalies routes are never called by the frontend (grep finds no risk/anomalies usage in static/) and no test hits the HTTP routes — only the underlying life_stats.habit_failure_risk / health_anomalies / health_baseline functions are unit-tested directly. The route wrappers are unreachable dead endpoints.
    FIX: Either wire them into the UI (e.g. a risk badge on the habit card, anomaly dots on the health chart) so the computed insight is actually surfaced, or remove the two route wrappers since the tested logic lives in services/life_stats.py.

### read-books (4)
- [med/functional] static/js/read.js:139 (input handler) + 36-43 (loadRead/_render)
    WHAT: The saved-items search box (#read-q) debounces a call to loadRead(), and loadRead -> _render() replaces the ENTIRE read-body innerHTML, destroying and recreating the input. The value is preserved (rendered with value=...), but focus and caret position are lost. So after you pause typing for 300ms, the input is recreated unfocused; resuming typing does nothing until you click back into the box. Very noticeable while searching.
    FIX: In loadRead(), before fetching, record whether document.activeElement.id === 'read-q' and its selectionStart; after _render(), re-focus #read-q and restore the caret with setSelectionRange. (Applied.)
- [med/functional] static/js/books.js:137-143 (doSearch)
    WHAT: In the add-book form, typing a title/author and then clicking 'search' (the autofill lookup) calls _render(), which rebuilds the whole form and wipes the already-typed #book-title and #book-author inputs. The #book-q query box is also cleared after a search, so refining the query means retyping it. Loss of user-entered input.
    FIX: Capture #book-title/#book-author values (and the query q) before _render() in doSearch, then write them back onto the freshly rendered inputs, the same way _renderKeepForm restores fields on the pick path. (Applied.)
- [low/dead] routes/read.py:70,83-84 (list_items tag param)
    WHAT: list_items accepts a `tag` query param and filters ReadItem.tags.ilike(...), but no frontend ever sends /api/read?tag= (read.js builds params only from _filter and _q). read.js displays tags as '#feed' chips but they are not clickable and there is no tag-filter UI, so this code path is unreachable from the app.
    FIX: Either wire the displayed tag chips to set a tag filter (click '#feed' -> reload with ?tag=feed), or drop the `tag` param + filter branch from list_items. Recommend wiring it up since tags are already shown.
- [low/visual] static/js/books.js:52 vs 108
    WHAT: Inconsistent labels for the same shelf. The per-card move buttons label the 'done' shelf as 'finished' (line 52: k==='done' ? 'finished'), while the add-form status segment labels the same 'done' shelf as 'read' (line 108: k==='done' ? 'read'). Same destination shelf, two different words shown to the user.
    FIX: Pick one label for the done shelf and use it in both places (e.g. 'read' everywhere, matching the shelf header 'read' from SHELVES).

### days (2)
- [med/functional] routes/days.py:125
    WHAT: _fmt() unconditionally calls ev.created_at.isoformat(). A DayEvent row with NULL created_at (possible from an import/migration/manual insert — the column only has a Python-side default, not a DB NOT NULL) makes _fmt raise AttributeError, which 500s GET /api/days and blanks the entire days view. Confirmed by inserting a raw NULL-created_at row: the endpoint throws 'NoneType' object has no attribute 'isoformat'. Note line 104 already guards created_at for the progress calc but line 125 does not.
    FIX: Guard it like line 104 does: "created_at": ev.created_at.isoformat() if ev.created_at else None,
- [med/functional] routes/days.py:209-212
    WHAT: update_day (PATCH) sets name with no emptiness check and no strip, so a PATCH with name="" or "   " blanks the event name and returns 200. The edit card's save sends name: v('name')?.trim(), so a user who clears the name field and hits save ends up with a nameless card (.day-name renders empty) with no way to identify it. This is inconsistent with create_day, which rejects blank names with 400 and stores name stripped. Confirmed: POST blank name -> 400, but PATCH blank name -> 200 with name now ''.
    FIX: In update_day, before the field loop, validate name when provided: if body.name is not None and not body.name.strip(): raise HTTPException(400, "name required"). And store stripped (set ev.name = body.name.strip()) to match create.

### subs (5)
- [high/functional] routes/subscriptions.py:599
    WHAT: undo_payment sets sub.last_posted_due = "" (empty) and moves next_due back to the paid date. For an account-linked sub this wipes the idempotency guard in _roll_and_post: the very next app open (GET /subscriptions) or background check_renewals rolls next_due forward from last.date and, because last_posted_due is now empty, re-posts a real money Transaction for EVERY due date in that span (the `iso <= last` guard is always False against ""), up to _POST_CAP=36. Result: undoing one late payment silently injects duplicate auto-renewal charges into the money ledger the next time the app is opened — data corruption / double-charge.
    FIX: Don't clear last_posted_due to empty. Set it to the due date immediately before the undone one so only last.date can re-post. e.g. look up the prior SubPayment date for this sub (the next .order_by(created_at.desc()) after deleting `last`) and set sub.last_posted_due = prior.date if it exists else "". Or compute the previous cycle date: last_posted_due = (last.date minus one cycle). This keeps already-posted historical charges from being re-posted on the next roll.
- [med/functional] routes/subscriptions.py:66-101 (_roll_and_post) vs 511-524 (mark_paid)
    WHAT: _roll_and_post advances next_due past today for ALL active overdue subs, including ones with no account_id, on every GET /subscriptions. For a non-account sub that went overdue (user opened the app a day late), next_due jumps to the future so payable becomes False, the 'paid' button disappears (subs.js:202), and NO SubPayment row is logged. The user loses the ability to record/track that renewal — paid_count and payment history silently skip it even though the charge really happened.
    FIX: Only auto-advance next_due inside _roll_and_post for subs that actually auto-post (account_id linked). For non-account subs, leave next_due in the past so the row stays payable and the user can mark it paid (which logs a SubPayment). Alternatively log a SubPayment for each rolled-over charge so history stays complete.
- [med/functional] routes/subscriptions.py:66-101 (_roll_and_post) — no SubPayment recorded
    WHAT: Account-linked subs auto-post a Transaction on rollover but never create a SubPayment row. Because of that, the payment-history button '⤺ N' (subs.js:204, gated on paid_count) never appears for account-linked subs, and the undo-payment flow (which only operates on SubPayment rows) can never undo an auto-posted charge. A wrong auto-renewal charge has no in-app undo path.
    FIX: In _roll_and_post, when a Transaction is posted, also db.add a SubPayment(sub_id, date=iso, amount, txn_id=txn.id) so auto-posted renewals show up in history and can be undone like manual ones.
- [low/dead] routes/subscriptions.py:375-381 (/subscriptions/overlaps) + services/sub_overlap.py
    WHAT: The /api/subscriptions/overlaps route (and the redundant-service overlap feature it powers) is never fetched by the running app — subs.js has no call to it (only duplicates, unused, detect, upcoming, forecast, analytics are wired). It is exercised only by services-level tests, so the whole overlap UI capability is unreachable for the user.
    FIX: Either surface overlaps in subs.js (render a warning like the dup/unused chips) or remove the unused route to drop dead surface. If keeping for future use, that's fine, but right now no user can ever see overlap warnings.
- [low/visual] static/js/subs.js:87
    WHAT: The analytics chart title interpolates a.monthly_total and a.yearly_total as raw numbers (e.g. '$12.5/mo'), while every per-category bar value below uses .toFixed(2) ('$12.50'). Inconsistent money formatting in the same widget — totals can render with one decimal or none while the rows show two.
    FIX: Format the header totals the same way: `${esc(a.currency)}${a.monthly_total.toFixed(2)}/mo · ${esc(a.currency)}${a.yearly_total.toFixed(2)}/yr`.

### files (9)
- [high/functional] routes/files.py:420-427 (rename) + services/files_store.py:124-131
    WHAT: Renaming a file onto a path that already exists crashes. fs.rename does a bare src.rename(dst); on Windows this raises FileExistsError (a subclass of OSError, not FileNotFoundError/ValueError), so the route returns a 500 'Internal Server Error' and the user just sees 'rename failed'. On POSIX the same call silently overwrites/clobbers the destination file with no confirmation = data loss. Verified live: rename a.txt -> b.txt returned 500.
    FIX: In files_store.rename, guard the collision before the rename: `if dst.exists() and dst != src: raise ValueError('a file or folder with that name already exists')`. Keep the existing FileNotFoundError path. That converts the 500 into a clean 400 the frontend already toasts, and prevents the POSIX clobber.
- [high/functional] routes/files.py:420-427 (rename) / 430-441 (delete)
    WHAT: Rename and delete orphan all of a file's metadata. FileTag (tags+star+color), FileComment, and FileVersion rows are keyed on the relative path, but rename/delete never update or remove them. Verified live: starred+tagged a.txt, renamed to c.txt -> c.txt lost its star and tags, and the 'starred' smart folder still lists the now-nonexistent a.txt. Deleting b.txt left its tag in /by-tag and /tags/all forever. So stars/tags/comments/versions silently vanish on the most common operations, and dead entries accumulate in smart folders.
    FIX: On rename: update FileTag/FileComment/FileVersion rows where path == old (and for a dir, where path startswith old+'/') to the new path. On delete: delete the matching FileTag/FileComment rows (and ideally FileVersion blobs) for that path/prefix. Both routes already take `db`, so do it in the same transaction.
- [med/functional] routes/files.py:407-412 (mkdir) + services/files_store.py:107-110
    WHAT: Creating a folder whose name collides with an existing FILE crashes with a 500. p.mkdir(parents=True, exist_ok=True) only swallows the error when the existing path is already a directory; when a file sits there it raises FileExistsError, which the route doesn't catch. Verified live: mkdir 'c.txt' (an existing file) -> 500 Internal Server Error instead of a clean message.
    FIX: Catch the collision in fs.mkdir: `if p.exists() and not p.is_dir(): raise ValueError('a file with that name already exists')`, or broaden the route's except to also handle OSError -> HTTPException(400).
- [med/perf] static/js/files.js:74 (_renderQuota call) + routes/files.py:142-160 (/quota)
    WHAT: loadFiles() calls _renderQuota() on EVERY folder navigation, and /quota does base.rglob('*') + stat() over the entire vault tree each time. So every single folder click re-walks and stats every file in the whole vault just to redraw a disk bar whose value (free disk space) doesn't even change between clicks. On a large vault this makes navigation visibly laggy.
    FIX: The bar is driven by total/free DISK space, not folder contents — fetch it once on first load (or cache the result for ~30s) instead of on every loadFiles. Or move the quota fetch out of loadFiles and only refresh it after upload/delete.
- [med/visual] static/js/files.js:381-439 (editTags / openVersions / openComments)
    WHAT: The tag/version/comment popovers have no outside-click or Escape dismissal — they only close via their own cancel/close button. Worse, editTags removes any existing .file-tagpop globally (line 383) but openVersions/openComments only remove the popover within the SAME row (row.querySelector at 411/434). So opening a comment popover on one row and then a versions popover on another row leaves both open stacked on screen, and clicking empty space never closes them.
    FIX: Use a single shared dismissal: before opening any popover, remove every existing .file-tagpop in the list (document.querySelectorAll), and add a one-shot document click listener (and Escape) that closes the open popover when the click target is outside it.
- [low/perf] routes/files.py:26-35 (_tag_map) + 47-52 (_comment_counts), called from list_files:66-67
    WHAT: Listing any folder loads the ENTIRE FileTag table (db.query(FileTag).all()) and the ENTIRE FileComment table (every row) into memory just to decorate the handful of items in the current directory. As tags/comments grow this is a full-table scan on every list request even for a folder with two files.
    FIX: Filter to the current listing: collect the item paths first, then query `FileTag.path.in_(paths)` and `FileComment.path.in_(paths)` (a grouped count for comments). Same decorate output, bounded by folder size instead of total tagged/commented files.
- [low/functional] static/js/files.js:381-408 (editTags) / 507-512 (deleteEntry), reached from renderList used by openStarred:165
    WHAT: editTags and deleteEntry both call loadFiles() (no arg) on success, which always reloads the current FOLDER browser. But these actions are also wired up in the Starred and (for delete) search/smart views via renderList. Editing a tag or deleting from the Starred view yanks the user out of Starred back to the root/current folder instead of staying in the view, even though a _rerender hook already exists for exactly this.
    FIX: After a successful tag-save / delete, call `_rerender ? _rerender() : loadFiles()` instead of unconditionally loadFiles(), so smart/starred/search views refresh in place.
- [low/functional] static/js/files.js:12-19 (_writeUrl) + 623-626 (initFiles)
    WHAT: Sort direction is never persisted to the URL. _writeUrl writes p and sort but not order, and initFiles restores sort from the URL but not order. So if you sort by name-descending (or flip date to ascending) and then refresh or share the deep link, the direction silently reverts to the default.
    FIX: In _writeUrl set/delete the 'order' param alongside 'sort'; in initFiles read 'order' and apply it to _order when it's 'asc'/'desc'.
- [low/functional] routes/files.py:90-106 (set_tags)
    WHAT: PUT /tags with empty tags and empty color creates a brand-new all-empty FileTag row (path with no tags, no color, not starred). These useless rows then get walked by _tag_map on every list call and by /tags/all, adding noise/scan cost for nothing. Verified live: PUT tags {tags:[],color:''} on a fresh path inserts a row.
    FIX: After applying the body, if the resulting row has no tags, no color, and starred is false, delete the row instead of committing it (or skip creating one when there's nothing to store).

### aide-chat (5)
- [high/functional] routes/sessions.py:203
    WHAT: edit_message deletes trailing messages with `Message.timestamp > msg.timestamp` (strictly greater). On this Windows host datetime.utcnow() has coarse resolution — back-to-back calls are 100% identical (measured 2000/2000). The user message and its assistant reply are saved in the same _stream_and_save commit, so they get the SAME timestamp. Result: editing (or regenerating, which hits the same endpoint via sessions.js:532) a user message does NOT delete the stale assistant reply that shares its timestamp. Verified live: after POST .../messages/{uid}/edit the old assistant turn stayed, and message_count became wrong (reported 1 while 2 rows existed). On reload the orphaned old reply reappears and is fed back to the model as history — silent data corruption of the conversation, plus a drifting sidebar count.
    FIX: Don't rely on a strict timestamp comparison that can't break ties. Either delete by `(Message.timestamp > msg.timestamp) OR (Message.timestamp == msg.timestamp AND Message.id != msg.id)` while excluding msg itself, or fetch the messages ordered by (timestamp, id) and delete every row positioned after target (using the relationship order the user actually sees). Recompute message_count from the surviving rows after deletion instead of subtracting len(later).
- [low/dead] routes/chat.py:630-639
    WHAT: GET /api/sessions/{session_id}/messages/{msg_id}/artifact/{idx} (get_artifact) has no caller. Artifacts are extracted client-side (artifacts.js extractArtifacts), stashed in wrap.dataset.artifacts, and reopened via openArtifactFromMsg reading that dataset — never this route. Grep across all JS/tests finds zero references. Dead route shipping an unused DB-reading endpoint.
    FIX: Delete the get_artifact route (and its now-unused _ART_RE/_extract_artifacts on the backend if nothing else uses them — note _extract_artifacts is still used in _stream_and_save to persist meta.artifacts, so keep that; just drop the route).
- [low/dead] routes/chat.py:570-572
    WHAT: GET /api/chat/status/{session_id} (chat_status) returns {streaming: ...} but has no caller anywhere in JS or tests (verified by grep). Reconnect/progress is handled by /api/agent/runs/active in bgrun.js, not this route. Dead endpoint.
    FIX: Remove the chat_status route.
- [low/dead] static/js/chat.js:94
    WHAT: `let _bgStreams = new Map();` is declared with a comment ('session_id -> partial accumulated text') but is never read or written anywhere in the file (grep finds only the declaration). Dead variable left over from an earlier background-stream design.
    FIX: Delete the _bgStreams declaration.
- [low/functional] static/js/chat.js:573-587 vs static/js/sessions.js:360-363
    WHAT: The action-button set on a freshly streamed assistant message and the same message after reload are inconsistent. Live: copy, +note, +task, shorter, simpler. Reloaded (appendAiMsg + renderMessages): copy, regen, shorter, simpler, branch — but no +note/+task. So +note/+task vanish after reload and regen/branch only appear after reload. Same message offers different actions depending on whether the page was refreshed, which is confusing.
    FIX: Build one shared action-row helper used by both the live finalize in chat.js and appendAiMsg in sessions.js so the button set (copy, regen, +note, +task, shorter, simpler, branch, open-artifact) is identical in both paths. Also set row.dataset.msgId on the streamed AI row once saved so its rewrite/branch target the right message before a reload.

### home-system (3)
- [med/perf] static/js/watch.js:28
    WHAT: The watch poll only stops when the whole browser TAB is hidden (`document.hidden`), never when you navigate away from the watch view inside the SPA. After visiting watch once, the 15s interval keeps firing forever in the background while you're on any other view — each tick refetches three endpoints (/api/watch/overview, /api/usage/summary, /api/status/config) and rewrites the hidden #watch-body DOM. system.js avoids this by checking `v.style.display === 'none'` in its tick(); watch.js doesn't.
    FIX: Gate the poll on the view being visible, like system.js does. In the interval callback (and in `_refreshQuiet`), early-return when the watch view isn't shown: `if (document.getElementById('watch-view')?.style.display === 'none') return;` before calling `_refreshQuiet()`.
- [med/functional] routes/watch.py:214-221
    WHAT: `uptime_24h` / `uptime_7d` are computed only from the last KEEP=200 checks (record_check prunes each monitor's history to 200 rows). With the default 300s interval, 200 checks span ~16.6 hours, so the card's '7d' uptime can never actually reflect 7 days, and even '24h' is truncated whenever the interval is short. The label promises a window the stored data can't cover, so the percentages are silently wrong/misleading.
    FIX: Either compute uptime from a time-bounded query instead of the KEEP-capped slice (e.g. `db.query(MonitorCheck).filter(monitor_id==m.id, ts>=week_ago)` for the 7d figure, separate from the spark slice), or raise KEEP / prune by age so 7 days of checks are actually retained, or relabel the card to the real rolling window.
- [low/dead] static/js/app.js:544-564,591
    WHAT: The `withDay` branch of `_wireHomeAsk` (the day-summary fetch at 553-564) and its trigger `document.getElementById('ha-day')?.addEventListener('click', () => ask(true))` at line 591 are unreachable: the #ha-day button was removed from index.html (test_home.py:85 asserts it's gone), and the only reachable callers — the send button and Enter key (lines 589-590) — call `ask(false)`. The day-rundown now lives entirely in the today-strip's #ht-ask button via _askAideAboutToday. So `ask(true)`'s whole code path is dead.
    FIX: Remove the `ha-day` listener (591) and the `if (withDay) {...}` block (553-564), simplifying `ask` to the plain-message path. Update the now-stale test_home.py assertions (lines 88-91) that require `ask(true)` / `if (withDay)` to still exist, since they only pass because of this dead code.