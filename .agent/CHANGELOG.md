# project changelog

## 2026-07-31

Change: added the literal KOKUEN control census and repaired the first regressions exposed by rendered
per-control measurement.

Files affected: source-derived control census, semantic overrides, generated JSON/Markdown, census
tests; shared KOKUEN target and busy-state runtime; Andromeda switch names, persistence rollback, and
focused source contracts; and current project state.

Reason: the feature-level map did not identify every individual static or rendered control and activation
path. The new 1,608-record census keeps owners, handlers, events, states, evidence, and uncertainty
machine-verifiable. Real browser measurement found sub-44px controls and unnamed Andromeda switches;
failed switch writes also left visible state inconsistent with persistence. Shared 44px hit geometry,
busy repeat rejection, explicit names, rollback, and retry feedback now make those failures honest.

## 2026-07-30

Change: completed the KOKUEN v5 whole-application interaction and anti-slop audit and repaired every
confirmed final-surface regression before the completion checkpoint.

Files affected: shared entrance motion and menu control; Home and Docs hover treatment; Aide saved-session
rows and action menu; shell localization and cache identity; Journal year grid and mobile toolbar;
finished-surface, Library/Health, Home, Docs,
design-system, manifest, PWA, and source regressions; and current project state.

Reason: keep Home, streamed text, drawers, and settings content visible before animation; remove
template-like hover motion, the photo-person hover jump, and decorative glow;
show one localized app identity, prevent mobile month labels and field copy from clipping or overlapping,
enforce 44px Journal controls, make saved Aide sessions and their custom menu keyboard-complete,
separate session, star, project-toggle, and project-delete actions so interactive controls never nest,
distinguish concurrent browser mutations exactly, and require the latest real rendered application
rather than stale source expectations as the completion authority.

Change: completed the KOKUEN v5 migration through Settings, first-run setup, the installable PWA, and
the browser Passwords extension, and added an executable exhaustive interaction logic map.

Files affected: setup choices and busy behavior; PWA manifest, cache identity, exact import precache
generation, and cold-offline gate; browser extension state, styling, forced inactivity-race recovery,
and unpacked-extension gate; interaction logic source, resolver, generated JSON/Markdown, reconciliation
tests; mobile specialist tabs; and current project state.

Reason: preserve full keyboard and repeat-safe behavior outside the main shell, prove exact current
modules work from a cold offline install, close the real pairing marker race without widening browser
authority, prevent mobile tab labels from being sliced, and keep every registered interaction surface,
state transition, recovery path, proof, and external gap machine-verifiable.

Change: migrated and repeatedly verified Home, Aide, and Andromeda against the frozen KOKUEN v5 shell.

Files affected: Aide mobile identity markup; primary-space rail-clearance styling; Home, Aide,
Andromeda, combined Phase 4, finished-surface, and v5 source regressions; and current project state.

Reason: keep one visible identity and one universal launcher in each primary space, prevent the rail
from clipping Andromeda's phone settings and footer controls, and replace stale test paths that depended
on removed local Home and Apps buttons. Focused and combined gates passed repeatedly on isolated data;
no push occurred.

Change: established the KOKUEN v5 universal runtime and shell foundation in the real application.

Files affected: shared JavaScript control runtime and custom select; universal rail, sheet, destination
registry, app identity markup and styling; component tokens and machine-readable contracts; design-system
sources and decisions; focused JavaScript/Python regressions; and current project state.

Reason: give every space one predictable cross-app path, remove duplicated Home/profile/breadcrumb
chrome, enforce the 44px and 14-state contracts without a framework migration, and freeze the shared
foundation before primary and specialist migrations. The isolated real app passed shell navigation,
focus return, and fresh rendered inspection; no push occurred.

Change: recovered, reviewed, freshly verified, and locally checkpointed the KOKUEN v3 baseline before
starting the approved KOKUEN v5 rebuild.

Files affected: KOKUEN v3 tokens and design records; universal command and setup behavior; registry,
source, JavaScript, Python, and real-browser regressions; and current task state.

Reason: preserve the proven baseline with the required local commit identity, begin v5 from a clean
worktree, and keep the new mission, migration order, proof gates, and no-push boundary explicit.

## 2026-07-29

Change: aligned the tracked Alles design system with KOKUEN v3, rebuilt universal command as a complete
accessible command surface, and fixed first-run setup arbitration discovered during real-browser testing.

Files affected: design-system tokens, foundations, components, patterns, QA and decisions; the project UI
skill; universal command markup, behavior, styling and cache identity; setup wizard; feature registry and
generated catalogs; focused JavaScript/Python contracts; and the finished-surface Playwright gate.

Reason: adopt the new portable spacing/component contract without inflating existing geometry, make
command search deterministic for pointer and keyboard users, prevent asynchronous setup from stealing
focus, and require current rendered proof rather than a standalone mockup. No files were staged,
committed, or pushed.

## 2026-07-28

Change: made the Ubuntu CI contracts deterministic by declaring the intended launchd, lsof, Unicode
casefolding, and safe-network conditions inside their tests instead of inheriting the runner platform or
DNS state.

Files affected: Server policy, Andromeda evidence, local source-claim migration, and managed Actual test
modules.

Reason: the complete macOS suite passed, but GitHub's Python 3.11 Ubuntu runner correctly selected Linux
branches and rejected unresolved example hosts in six tests that meant to exercise other bounded paths.
The four affected modules now pass all 130 tests on both local Python 3.14 and isolated Python 3.11.

Change: captured the completed Afterlife delivery in one owner-authorized local commit while keeping the
remote untouched.

Files affected: the verified accumulated Afterlife implementation, migrations, tests, interface,
feature registry, generated credits and license notices, and project knowledge.

Reason: preserve the exact locally verified state after the owner clarified that local Git history was
desired but no GitHub push was authorized. Private data and every `AGENTS.md` remain outside the commit.

## 2026-07-27

Change: finished every locally actionable completion-registry row, corrected the shared specialist
sidebar, Andromeda decisive-answer emphasis, Aide News navigation, and phone gutter alignment, then
recorded evidence for every feature and expanded model routing to all 120 registered tasks.

Files affected: shared application navigation and KOKUEN styling; Andromeda and Aide News behavior;
cache identities and focused tests; feature registry, generated catalogs, model routing, and project
knowledge.

Reason: the completion matrix must have no unchecked or partial work, model/effort policy must survive a
feature becoming passed, and visible work must be exercised in the real isolated application before
handoff. External credentials, services, devices, Linux, and isolated-network scenarios remain honestly
blocked. No files were staged, committed, or pushed.

## 2026-07-26

Change: completed the approved feature-registry implementation program, repaired the Gallery active-state
regression, reconciled current routes/tables/jobs, ran the full regression and real-interface gates, and
recorded honest passed, blocked, and unchecked acceptance instead of claiming external proof.

Files affected: feature registry and generated evidence; Files, Finance, model-authentication, Aide,
managed-companion, Gallery, Server, credits, route/inventory documentation, focused and repository-wide
tests, task state, and engineering records.

Reason: the owner required every planned capability to map to implementation and tests, while live
provider, bank, device, Docker/network, and lifecycle acceptance must remain visible until disposable
credentials or infrastructure exist. No files were staged, committed, or pushed.

Date: 2026-07-26

Change: added explicit legitimate model-provider authentication and the documented Gemini desktop OAuth
flow, including encrypted refresh credentials, PKCE/state, loopback callback, periodic refresh, revoke,
provider quota/revocation metadata, owner-managed proxy isolation, and the responsive Settings surface.

Files affected: model endpoint schema and migration; provider authentication, catalog and LLM routing;
model routes and scheduled jobs; Settings markup, behavior and styling; registry/catalog; focused API,
migration, LLM and real-browser tests; task state; and engineering decisions.

Reason: provider connections must state exactly which credential type they use, never import consumer
sessions, remain usable after token expiry, and show honest billing and revocation boundaries.

Date: 2026-07-26

Change: shipped durable selectable Aide questions across the agent runtime, Jarvis, Discord, and the real
KOKUEN conversation surface; fixed the shared phone drawer and fresh Auto default; and completed the
required generated credits inventory.

Files affected: Aide question normalization/runtime/routes/UI/tests; Jarvis persistence, migration,
Discord and handoff paths; shared navigation and mode state; credits sources, generator, generated
notices and tests; feature registry; task state; and engineering decisions.

Reason: the completion program requires one reload-safe question contract, honest model continuation,
consistent responsive behavior, and complete source/inspiration evidence before expanding provider and
app-wide tool coverage.

Date: 2026-07-26

Change: removed the standalone-starter approval gate from the active interface workflow while retaining
real rendered verification as the completion gate.

Files affected: global, repository, and active-worktree agent rules; current task state; and engineering
decisions.

Reason: the owner explicitly requested fully autonomous implementation. Starters remain optional design
tools, but no longer pause runtime work for separate approval.

Date: 2026-07-26

Change: started the approved exhaustive completion program with an authoritative feature/test registry,
repaired the stale Files real-browser gate, and built the exact KOKUEN approval starter for the remaining
material interface changes.

Files affected: feature registry loader, registry JSON, generated feature catalog and acceptance matrix,
runtime reconciliation tests, Phase 7 Files browser gate, standalone completion starter and browser gate,
task state, decisions, changelog, and wiki.

Reason: implementation and verification must reconcile against every product surface and remain honest
about missing work. The Files gate still targeted a hidden pre-consolidation control, while the remaining
Files, Aide, provider, Finance, and Server interfaces require an approved exact starter before runtime work.

Date: 2026-07-25

Change: corrected the finished interface system after the owner found repeated spacing, focus, and sidebar
failures. Added one shared persisted navigation toggle to all nine workbenches, normalized KOKUEN spacing to
its documented scale, removed Aide's doubled purple composer focus treatment, and fixed Kanban's cramped and
clipped desktop columns.

Files affected: specialist workbench shell markup and runtime state, KOKUEN runtime CSS, shell/cache
identities, focused JavaScript and rendered Playwright gates, design-system decision 0007, and task state.

Reason: the defects came from shared shell and spacing rules, not isolated app mistakes. The correction is
now covered across all nine workbenches at desktop and phone widths with keyboard and real-browser checks.

Date: 2026-07-25

Change: completed the Phase 12 capstone with a clean isolated repository-wide regression, a bounded
slow-host recovery allowance, refreshed implementation inventory locks, and the final point-by-point
rendered design-system and anti-slop review.

Files affected: recovery CLI and regression coverage; Phase 0 data and route inventories; Phase 12 task,
plan, and design-review evidence; and project changelog.

Reason: make the final claim depend on current implementation truth and a complete green suite, while
avoiding false migration failures on a heavily loaded host. No files were staged, committed, or pushed.

Date: 2026-07-24

Change: corrected the live Phase 12 delivery path by refreshing the shell, stylesheet, Andromeda module,
and offline-cache identities; restarted the isolated `6769` preview with the current backend; and split
neutral keyboard focus from the purple active-state token across KOKUEN runtime and source documentation.

Files affected: shell and service-worker cache identities, KOKUEN runtime/tokens/foundations/decision,
Andromeda module identity, focused contracts, and current task state.

Reason: the source edits were not visibly reaching the already-open live page, and the finished-surface
focus rule still overrode the approved neutral Phase 12 focus with the rejected purple rectangle.

Date: 2026-07-24

Change: narrowed Andromeda highlighting to a verified decisive answer span, removed divider lines between
normal results, and bound the Phase 12 Server direction to the existing Neofetch/btop renderer.

Files affected: Andromeda evidence contract and renderer, focused Python/JavaScript/browser tests, standalone
Andromeda and Server snapshots, Phase 12 plan and review, accepted result-quality decision, and task state.

Reason: the owner wanted Google-like emphasis on `3.50.4`, open unruled result rows, and the real existing
Neofetch/btop graphics rather than another approximation.

Date: 2026-07-24

Change: separated ordinary Andromeda links from the AI answer and expanded the Phase 12 Server process
monitor to consume the full btop width with a longer process list.

Files affected: real and standalone Andromeda results composition, Phase 12 Server starter, focused
rendered gates, phase plan, design review, and task state.

Reason: the owner wanted normal web results to read as an independent source stream and rejected the dead
space beside the short process panel.

Date: 2026-07-24

Change: corrected the Phase 12 Andromeda composition and restored the original machine-monitor character to
the Server direction.

Files affected: the real and standalone Andromeda shells, Phase 12 Server starter, focused browser gates,
phase plan, design review, and task state.

Reason: the owner rejected Andromeda's logo column, misaligned/narrow search composition, and non-terminal
Home placement, and explicitly required the original neofetch and btop views to survive Server consolidation.

Date: 2026-07-24

Change: recorded the owner's approval of the exact Phase 12 starter set and opened runtime implementation.

Files affected: Phase 12 design review and current task state.

Reason: the approved fake-data screens are now the binding implementation target; later visual changes still
require their own review.

Date: 2026-07-24

Change: restored the earlier grouped Apps composition and changed the Phase 12 product map from eight to
nine specialist destinations, with Library remaining visible. Removed the inherited row overhang that
exposed a horizontal scrollbar in the corrected directory.

Files affected: the standalone workbench starter and gate, Phase 12 plan and design review, changelog, and
task state.

Reason: the owner prefers the established full-screen Apps format and wants consolidation to reduce the
directory to nine apps rather than replace its presentation or bury Library inside Docs.

Date: 2026-07-24

Change: built and verified the exact standalone Phase 12 approval starters without touching runtime code.

Files affected: the isolated workbench, Andromeda, and Server starters; their rendered behavior gate;
the Phase 12 design review; the phase plan; and task state.

Reason: prove the consolidated navigation, one-search Andromeda flow, independent verification controls,
secure Server policy UX, responsive back paths, custom controls, and KOKUEN focus split before any material
real-app rework. Owner approval of these exact files remains required.

Date: 2026-07-24

Change: opened Phase 12 with the then-approved eight-workbench map, two-stage Andromeda verification,
universal navigation/focus correction, and fail-closed Server management policy.

Files affected: Phase 12 implementation plan, phase indexes, engineering decisions, and task state.

Reason: the live product still exposes fifteen specialist destinations, inconsistent shells, a missing
Gallery Home path, a loud purple full-field focus outline, a bare Andromeda shell, and a System screen
that monitors more than it manages. Real UI changes remain behind exact standalone-starter approval.

Date: 2026-07-23

Change: completed the final current-snapshot autoreview rerun, closed every accepted finding, and
finished the repository-wide verification gate under the owner's lower-cost review configuration.

Files affected: native rollback; Actual staging and bridge handling; Finance identity; Files local,
remote, upload, trash, and offline operations; WebDAV transport and conditional downloads; Journal
migration; Docs cleanup; model persistence; focused regressions; generated notices; and task state.

Reason: keep crash recovery bound to mutations that actually happened, preserve exact external and
filesystem identity, prevent blocking FIFO substitutions and unbounded uploads, make test isolation
independent of local DNS, and require ETag-bound downloads to fail closed. Final evidence is 5,469
Python passes with 15 skips, 527 JavaScript passes, clean Ruff lint and formatting, current generated
credits, clean syntax and diff checks, and zero accepted findings across every refreshed review scope.

## 2026-07-22

Change: closed every confirmed finding from the current five-shard autoreview snapshot and added
focused regressions for each new recovery, ownership, and UI-state boundary.

Files affected: native rollback, managed Actual and SearXNG, Actual staging and Finance import undo,
shared folders, offline and remote file operations, WebDAV and S3 identity, Vault transfer and Travel
Mode, Discord delivery, Plan and News state, browser-test isolation, KOKUEN selection styling,
focused tests, and task state.

Reason: retries must not duplicate or delete the wrong external state; destructive work must verify
its exact owner and version; vault moves must preserve late writes; UI failures must not claim that
an operation failed when only refresh failed; and browser gates must never follow redirects or read
owner sessions. Fresh focused evidence is 147 plus 119 plus 14 Python passes and 491 JavaScript
passes; full-suite and clean-review reruns remain before completion.

Change: closed every confirmed blocker from the complete first-pass review and added focused
regressions for the new safety and integrity boundaries.

Files affected: migrations, Files/offline/storage services, native distribution, setup, Notes and
Docs preservation, Actual subscription reporting, Discord and Aide provenance, localization,
managed services, release notices, isolated browser gates, focused tests, and task state.

Reason: upgrades, recovery, uninstall, offline copies, document edits, canonical currency history,
service retries, exposed access, message attachments, and browser verification must fail safely and
remain resumable. Fresh affected results are 457 Python and 141 JavaScript passes; full-suite and
clean-review reruns remain before completion.

## 2026-07-20

Change: moved the five new Phase 8 workbenches from the legacy route breadcrumb to app-owned shells.

Files affected: Plan, Inbox, Library, Health, and Finance group markup; KOKUEN shell styling and
navigation wiring; shell cache identity; focused JavaScript, manifest, and rendered browser gates;
design-system foundations, brand, components, patterns, responsive guidance, QA checklist, and
decision record; and task state.

Reason: the shipped Docs, Aide, and Files surfaces identify the app inside their own compact header
or rail and do not repeat `app / alles` above it. The five newer workbenches now follow that product
boundary with a visible app name, direct Home action, and local tabs. The app name appears once rather
than repeating as a giant landing title. Older apps retain the legacy shell until their separately
approved redesigns.

Change: completed and reconciled the Phase 8 delivery gate without autoreview, as directed by the
owner.

Files affected: managed Actual installation, Aide terminal materialization, shell cache stamps,
focused regressions, Phase 8 and pre-Phase-6 audits, phase status, and task state.

Reason: npm 11 needs local package executables while native dependencies install, but the published
Actual bundle must remain symlink-free; pending-folder activation must not close the terminal it just
materialized; changed browser modules need a new cache identity; and final status must use the exact
4,933-Python, 419-JavaScript, live Actual, browser, Ruff, warning, diff, and Git-index evidence while
making no clean-autoreview claim.

## 2026-07-19

Change: closed the five pass-22 and pass-23 Phase 8 review blockers and their direct variants.

Files affected: Actual canonical intent handling and staging cleanup, Andromeda pagination, Aide
terminal lifecycle and zoom browser proof, the pinned Actual bridge, cache identities, and focused
backend, JavaScript, live-runtime, and browser tests.

Reason: unfinished Actual account creation must not leak into normal reads or writes, filtered search
pages must keep later provider rows reachable, hidden terminals must not retain live sockets, failed
Python-side staging validation must remove its identified Actual budget, and the claimed 200-percent
zoom gate must exercise real reflow geometry.

Change: closed the six pass-21 Phase 8 review blockers and their directly related variants.

Files affected: Finance conversion and budget identities, restore validation, managed Actual install
trust, Aide task context recovery, browser loading/error probes, Files finished-surface assertions,
cache identities, and focused tests.

Reason: keep review/apply arithmetic identical, prevent assignment and cap deletion from colliding,
reject resurrected deleted assignments and symlinked executables, restore confirmed Aide context after
failed writes, and make browser gates prove current asynchronous and Phase 7 UI behavior reliably.

Change: closed the eight pass-20 Phase 8 review blockers with focused regression proof.

Files affected: canonical Actual reads and deletion state, Finance import locking, transfer edits,
migration reconciliation, managed rollback trust, service-worker mutation policy, grouped routing,
and focused backend, JavaScript, and browser tests.

Reason: keep pending deletions authoritative without resurfacing them, serialize import preview with
cutover, reject partial transfer mutation and contradictory FX arithmetic, bind caps and rollbacks to
exact trusted artifacts, keep Finance writes out of offline replay, and preserve the selected grouped
view on single-host reload.

Change: closed the seven reproducible pass-19 Phase 8 review blockers with focused regressions.

Files affected: reviewed Finance imports, Actual snapshot/reconciliation and transfer deletion,
specialist navigation promise handling, the approved Aide starter, and focused tests.

Reason: unknown currencies must never become reviewed conversion evidence, every migrated account
and subscription payment needs complete conversion proof, account parity must compare that proof,
transfer deletion must retain original link identities, and navigation must finish or surface its
real asynchronous work. The eighth report, a missing ORM column, was checked against both worktrees
and was already implemented.

Change: closed the six pass-18 Phase 8 review blockers with focused regression proof.

Files affected: Finance migration and currency evidence, managed Actual bundle/process trust,
specialist routing and cache identities, canonical-host documentation, and focused tests.

Reason: preserve the documented default currency when ledger state is absent, keep ambiguous labels
out of trusted evidence, reject contradictory FX records, refuse altered managed code, never signal
an unrelated process, and keep Aide reminders routable without colliding with Plan reminders.

## 2026-07-18

Change: closed the five pass-17 Phase 8 review blockers with focused regression proof.

Files affected: Finance foundation migration and Actual staging/readback, the Node/Python Actual
bridge boundary, managed Actual install trust and repair, the Docs KOKUEN rail, and focused tests.

Reason: a later migration attempt must not reuse an Actual deleted-import identity, arbitrary bridge
errors must not expose private data, transfer links need verified payees and reciprocity before they
are recorded, an incomplete managed install must be repairable, and the shipped Docs rail must match
the approved 244-pixel browser contract.

Change: reconciled the Phase 0 live inventory and trust-map baseline after Phase 8.

Files affected: data inventory, trust map, Phase 0 execution record, pre-Phase-6 audit, and their live
document regression.

Reason: Phase 8 adds six mapped Finance/Actual tables and two routers. Current evidence now records
113 mapped tables, 14 root roles, 78 routers, 814 routes, and 23 jobs rather than presenting the
pre-Phase-8 107-table/795-route checkpoint as current.

Change: closed interrupted-import, backup symlink-race, restore snapshot, and search-setting races.

Files affected: Finance import routes and UI, managed Actual backup/restore, Andromeda settings,
focused regressions, route snapshot, trust map, and task evidence.

Reason: give every claimed Actual transaction an explicit owner recovery path, make partial receipts
undoable, prevent backup traversal from following a link introduced during copy, restore only from
an immutable verified snapshot, and guarantee that the newest complete search tuple persists.

Change: closed the final pre-Stage-8 review blockers and completed the full readiness gate.

Files affected: Andromeda search and verified-answer handling, Home capture and Settings state
ownership, Discord draft handling, Files control sizing, isolated browser fixtures, their focused
regressions, and task evidence.

Reason: require explicit remote-model consent, prevent stale or duplicate work from replacing newer
state, keep configured result counts and verified claims honest, preserve edits made during pending
saves, enforce safe browser-test roots, and make every checked readiness claim rest on fresh full
Python, JavaScript, browser, Ruff, diff, and clean-autoreview evidence.

Change: fixed and live-verified the managed SearXNG Docker-share and restart-health boundaries.

Files affected: managed SearXNG service/routes, System and Andromeda lifecycle calls, focused
Python/JavaScript regressions, and task state.

Reason: Colima cannot mount every macOS temporary path, so install must fail before Compose with a
clear retryable state; start/restart must wait for loopback health rather than returning a false
success. A real isolated install, JSON search, restart, update, and uninstall gate now passes.

Change: regenerated and locked the current Phase 0 data/root inventory and trust map.

Files affected: the Phase 0 inventory, trust map, execution checklist, pre-Phase-6 audit, new live-
document regression, changelog, and task state.

Reason: the prior records stopped at 89 mapped tables and 741 routes and misdescribed external Vault
recovery, scoped bearer auth, managed SearXNG, and WebDAV/S3 Files. That pre-Phase-8 checkpoint derived
107 tables, 14 root roles, 76 routers, 795 routes, and 23 jobs from the real implementation; the later
Phase 8 reconciliation above supersedes those schema and route counts.

Change: created and browser-verified the standalone KOKUEN Home Settings starter.

Files affected: the Afterlife navigation mockup, its Playwright gate, changelog, and task state.

Reason: the remaining Phase 3 Home customization requirement needs an owner-approved design before
real interface work. The starter demonstrates section order and visibility, density, shortcuts,
keyboard access, responsive reflow, and light/dark states without touching the backend or owner data.

Change: aligned the Home starter toggles with the universal KOKUEN switch treatment after owner
review.

Files affected: the standalone Home Settings starter, its browser gate, changelog, and task state.

Reason: the initial square-track control did not match the rounded track and circular knob used by
shared KOKUEN settings controls. The corrected version keeps a 44-pixel target and all prior states.

Change: delivered the approved Settings-based Home customization flow and closed the remaining
pre-Stage-8 implementation-audit rows.

Files affected: the Settings Home pane, universal KOKUEN controls, Home rendering and preferences,
focused backend/JavaScript/browser coverage, specifications, phase trackers, and audit evidence.

Reason: let the owner safely order and show Home sections and shortcuts, choose density, keep Needs
you visible, and make every Phase 0-5 checked status match real implementation and current docs.

Change: completed the Files-owned header parity pass and closed the final local/S3 ownership,
recovery, and full-regression blockers.

Files affected: the Files header HTML/CSS/JavaScript and cache stamp; local file operations, storage
backends, S3 deletion, and System monitor boundaries; their focused Python, JavaScript, and browser
regressions; and the Phase 7 and pre-Phase-6 evidence records.

Reason: Files must use its approved app header at every responsive width, an operation must never
adopt or delete a same-byte replacement as its own object, S3 cleanup must require exact ownership,
and OS process text must not make the system-stats JSON response fail UTF-8 encoding.

Change: made restart recovery restore a quarantined race winner before refusing copy undo or local
delete cleanup.

Files affected: `services/file_operations.py`, `services/storage_backends.py`, and their focused
restart/quarantine regressions.

Reason: an existing quarantine is not proof of ownership. A retry must verify the stored publication
receipt and must not delete or indefinitely hide a same-byte replacement created by another actor.

Change: normalized local publication/tree identity read failures at the storage-receipt boundary.

Files affected: `services/storage_backends.py` and the same-root move restart regression.

Reason: an unreadable or disappearing destination must be handled as an ownership mismatch so an
armed move survivor restores the already removed source.

Change: made local copy publication and undo distinguish the exact published object from an
atomically replaced same-byte file.

Files affected: `services/file_operations.py`, `services/storage_backends.py`, and their focused
copy-undo and delete-race regressions.

Reason: byte equality is not ownership. Undo must remove only the object published by its operation
and must preserve a later replacement and that replacement's metadata.

Change: closed the remaining Phase 7 move-survivor, Trash rollback, restart-recovery, and disabled-
location cleanup races with focused regression coverage.

Files affected: `services/file_operations.py`, `services/share.py`, `services/offline_files.py`,
their local/remote/S3/share/offline regression tests, and the Phase 7 evidence records.

Reason: a failed move's verified survivor must remain usable for undo, restore and delete undo must
put verified bytes back in Trash when a concurrent replacement wins, restart must finish an armed
rollback, and internal cleanup must keep its claim even after the owner disables a location.

## 2026-07-17

Change: bounded remote offline downloads by both declared and actual bytes, recovered verified
pre-refresh cache snapshots after restart, gave active voice recording first ownership of Escape,
and preserved zero-byte Files labels.

Files affected: `services/offline_files.py`, `static/js/app.js`, `static/js/filesphase7.js`, and
their focused backend and JavaScript regressions.

Reason: remote metadata alone cannot enforce disk and cache limits, a crash after refresh
publication must not discard the last verified copy, one Escape press must cancel only recording,
and a valid empty file must not look like missing metadata.

Change: rejected descendant symlinks during local folder materialization and made the Apps drawer
close action a pure dismissal.

Files affected: `services/storage_backends.py`, `static/js/app.js`, and their focused Python and
JavaScript regressions.

Reason: local-to-remote copies must never escape a configured root, and closing a drawer must never
navigate away from unsaved work.

Change: added fail-first fixes for crash-safe local copy ownership, retryable cancelled-operation
location references, Aide route ownership, Files preview Escape state, and preview backdrop cleanup.

Files affected: `services/file_operations.py`, `services/storage_locations.py`, `static/js/app.js`,
`static/js/filesphase7.js`, and their focused Python and JavaScript regressions.

Reason: a process exit must not orphan a published copy, retryable work must keep its location, app
navigation must not inherit the wrong shell, and every preview close path must release media and
restore focus.

Correction: an earlier entry said cancelled operations release locations. Cancelled operations are
retryable, so they now keep their referenced location until the operation is purged or becomes
non-retryable.

Change: added fail-first fixes for rename rollback, target-scoped WebDAV mutation checks, permission
menu cleanup, Files preview Escape handling, and honest Gallery/Compare loading state.

Files affected: `services/file_operations.py`, `services/webdav_locations.py`, `static/js/app.js`,
and their focused Python/JavaScript regressions.

Reason: the closing autoreview found five cases where a failed operation or an early UI cleanup could
leave bytes, listeners, modal state, or loading state inconsistent with what the owner sees.

Change: fixed the next Phase 7 operation, recovery, and Files interface review findings with focused
regressions.

Files affected: file-operation retry and undo claims, default-location trash recovery, remote
temporary paths, Files derived views and downloads, storage-index tests, and their focused backend
and JavaScript coverage.

Reason: prevent duplicate workers, keep recovery out of the process directory, preflight metadata
before undo mutates bytes, bound remote filenames, remove stale or hidden actions, keep initial Files
loading responsive, avoid directory download failures, and remove test-order dependence.

Change: fixed the seven blockers found by the closing Phase 7 review pass.

Files affected: local and remote undo, WebDAV mutation safety, storage-location references, internal
transfer filtering, Files derived-view refresh, Starred search, preview focus handling, and focused
backend and JavaScript regressions.

Reason: keep metadata restorable after undo, support safe first writes to empty WebDAV collections,
release side-effect-free cancelled operations, show legitimate suffix-matching filenames, preserve
active derived views, and keep every interactive preview surface inside the keyboard focus trap.

Change: closed the last accepted Phase 7 review findings with focused regressions.

Files affected: public-share lookup, local and remote file operations, trash recovery, Starred Files
search, and focused backend, JavaScript, and browser tests.

Reason: keep legacy public tokens scoped to the default local location, reject stale destination
metadata before copy or move, recover generic vault trash after either crash boundary, and prevent a
global search response from leaking unstarred items into the Starred view.

Change: corrected the remaining Phase 7 review blockers before the completion gate.

Files affected: Files delete and restore metadata handling, trash cleanup, S3 version deletion,
storage-location references, Files refresh and offline search behavior, and focused regressions.

Reason: preserve every location-scoped metadata record across delete and restore, delete the exact
verified S3 version, keep retryable operations attached to their location, refresh either affected
location after an operation, and prevent late offline responses from dropping the active search.

Change: closed the Phase 7 data-publication, deletion-race, undo-metadata, receipt, and strict-offline
gaps found by the final review loop.

Files affected: storage backends, file operations, offline routes, the Files workbench, focused
backend and browser regressions, route compatibility, and Phase 7 evidence.

Reason: never expose partial destinations, never report an incomplete S3 folder deletion as success,
remove stale metadata and internal receipts, and keep offline work cache-only after the source is
unavailable.

Change: closed the final Phase 7 Files autoreview findings with regression coverage.

Files affected: storage backends and location references, Files-to-Photos import cleanup,
`static/js/filesphase7.js`, the Files preview markup, and focused backend, JavaScript, and browser
tests.

Reason: safely resume operation-owned partial folder uploads, remove failed per-item Photos output,
let terminal failed/cancelled history release a location, block read-only restores, make preview focus
modal-safe, and report mixed Photos imports honestly.

## 2026-07-18

Change: implemented the approved Phase 8 specialist workbenches and the staged managed-Actual Finance
boundary.

Files affected: specialist group routing/UI, currency and Actual migrations, reviewed Finance imports,
the pinned Actual bridge and lifecycle, canonical Finance services/routes, focused regressions, and
Phase 8 documentation.

Reason: reduce specialist app switching without merging records, preserve exact Finance evidence,
and move ledger authority only through a reconciled, backed-up, reversible cutover with no dual write.

Change: completed the Phase 8 architecture, integration research, official Actual throwaway spike,
and five standalone specialist-app approval starters.

Files affected: Phase 8 design/spike/bank/phase documents, fake-data Plan/Inbox/Library/Health/Finance
starters, their rendered browser gate, and task state.

Reason: define the real composition and Finance cutover boundaries before implementation, prove the
official Node path and recovery shape without owner data, and give the owner an exact responsive,
accessible KOKUEN direction to approve before the real interfaces change.

Change: eliminated the remaining Python datetime deprecations and SQLAlchemy unclosed-database test
warnings with regression coverage.

Files affected: UTC timestamp call sites across the app, explicit test engine/session cleanup, the
shared API test harness, and `tests/test_warning_hygiene.py`.

Reason: preserve Alles' existing naive-UTC database values without deprecated Python APIs, close
throwaway SQLite resources deterministically, and prevent either warning class from returning.

Change: completed the supported-Docker managed SearXNG lifecycle proof.

Files affected: `services/managed_searxng.py`, its regression tests, and the Phase 4 and pre-Phase-6
evidence records.

Reason: the live isolated run found that SearXNG's non-root user could not read the generated config
directory. The public config directory now has executable read traversal while the service root and
secret stay owner-only. Install, JSON search, limits, restart, failed-pull, rollback, update, and
uninstall-keep-data behavior now pass on supported Docker.

## 2026-07-16

Change: completed Phase 7 Files and storage locations.

Files affected: storage migrations and services, Files routes, `static/index.html`,
`static/js/filesphase7.js`, `static/kokuen.css`, Phase 7 tests, and Afterlife plan documents.

Reason: provide safe local and online Files locations, durable operations, explicit offline copies,
background indexing, and a verified multi-location Files interface.

Change: completed a fresh Phase 7 completion audit and fixed the gaps it found.

Files affected: `static/js/filesphase7.js`, `static/js/dialog.js`, the real Files browser gate,
JavaScript regression tests, and Phase 7 evidence records.

Reason: make file rows keyboard-operable, limit Files to Photos to supported content, make custom
dialogs focus-safe, stop completed operations and searches from replacing newer navigation state,
give loading and error states a recoverable Files layout, restore detail focus correctly, and keep
routine notices clear of the transfer panel.
## 2026-07-17

Change: corrected the Phase 7 delete boundary and the pre-Phase-6 implementation audit.

Files affected: Files delete routing and regression coverage, offline refresh recovery coverage,
Phase 0/3/5 checklists, the pre-Phase-6 evidence record, and task state.

Reason: prevent a post-completion same-path replacement from losing uncaptured metadata, prove that a
hard-stopped offline refresh restores its verified prior copy, and distinguish 222 current rows from
4 open and 12 superseded historical requirements.

Change: fixed four additional Phase 7 safety and refresh blockers found by autoreview.

Files affected: operation discard and Windows publication, storage-location references, filtered
Files refreshes, and their backend and JavaScript regressions.

Reason: keep recovery state for failed side effects, let owners finalize completed undo history,
support atomic folder publication on Windows, and prevent stale Starred or Offline results.

Change: fixed the latest five Phase 7 autoreview blockers with fail-first regressions.

Files affected: trash recovery, storage identity migration, operation discard, specialist request
accounting, Files transfer selection, and their focused Python and JavaScript tests.

Reason: preserve both copies after uncertain trash recovery, keep valid POSIX paths distinct, let
owners release terminal operation records deliberately, prevent overlapping app loads from corrupting
visible state, and prevent accidental same-path Copy or Move submissions.

## 2026-07-20

Change: implemented Afterlife Phase 9 native distribution, server-resumable setup, automatic local
protection backups, and paired exact-site Passwords browser access.

Files affected: native install/update/rollback/uninstall services and CLI, setup and Obsidian services
and routes, automatic backup scheduling, browser-connection migration/routes/service, Passwords and
setup KOKUEN UI, Manifest V3 extension, focused and rendered tests, and Phase 9 product documentation.

Reason: give macOS/Linux installs an owned recoverable runtime and let a browser fill one approved
login without receiving a permanent vault token or all-site authority.

Change: corrected Docs Home navigation and the real first-run entry point.

Files affected: Docs and app navigation modules plus focused JavaScript and Playwright regressions.

Reason: open the current Docs surface from Home, return from Docs to current Today rather than the
retired compatibility Home, and open resumable setup from the shipped Today entry point.

Change: closed the final Phase 9 regression and compatibility audit.

Files affected: recent-owner authentication, slash-command module versioning, cache and route
snapshots, the Phase 0 trust/data inventories, focused regressions, and Phase 9 status records.

Reason: prevent bearer/session credential confusion on high-impact actions, prevent duplicate Settings
module state, and keep the locked route, table, job, cache, and delivery records aligned with the real
Phase 9 implementation.

## 2026-07-21

Change: started Afterlife Phase 11 final review and handoff from the authoritative 41-request
brainstorm source.

Files affected: Phase 11 execution checklist, final-review ledger, Afterlife phase indexes, stale next
step, and current task state.

Reason: separate planning checkmarks from real delivery evidence and make the final requirement,
implementation, security, lifecycle, browser, and regression gate explicit before any completion
claim.

Change: closed the first two confirmed Phase 11 privacy and security-communication defects.

Files affected: HTML mail rendering, Aide Full access UI/runtime copy, focused JavaScript and Python
regressions, and current task state.

Reason: sandboxed email frames still allowed remote tracking resources to load, while Full access
claimed secret confinement even though an unsandboxed owner-approved shell has normal host access.
Remote email resources now fail closed and the shell boundary is stated honestly before use.

Change: removed the typed region field from first-run setup and made region detection automatic.

Files affected: the setup wizard, shell cache stamp, Phase 9 setup browser/source regressions, design
review, and task state.

Reason: region is browser environment state, not setup data the owner should have to type; timezone
remains visible for review and the server still validates the derived region before saving it.

Change: corrected Home's unclear and partially filtered app launcher.

Files affected: Home rendering and pinnable destinations, Settings language/focus handling, KOKUEN
styling, cache stamps, rendered regressions, and shipped behavior documentation.

Reason: the old Shortcuts label implied keyboard commands, its count was unexplained, and Phase 8 app
consolidation caused Calendar, Tasks, Mail, and Finance choices to disappear. The visible section is
now Pinned apps, every supported destination renders, Edit opens Settings, and focus survives an
immediate Home rerender.

Change: started Phase 10 from a current localization, credits, and hardening baseline.

Files affected: Phase 10 design review, baseline evidence, phase checklist, roadmap status, and task
state.

Reason: distinguish the existing English-only one-message foundation and partial acknowledgments from
the 41 still-open delivery rows before any translation or release-hardening claim is made.

Change: built and browser-verified the standalone Phase 10 language and Credits Settings starter.

Files affected: the fake-data KOKUEN starter, its browser gate, Phase 10 design/checklist status, and
task state.

Reason: give the owner a real eight-language, automatic-region, RTL, and manifest-shaped Credits
surface to test before any owner-facing Settings implementation begins.

Change: recorded explicit owner approval of the Phase 10 language and Credits Settings starter.

Files affected: the Phase 10 design review, delivery checklist, roadmap status, and current task.

Reason: the exact rendered starter is now the approved source for real Settings implementation;
unreviewed language catalogs remain unavailable until their independent gates pass.

Change: implemented and browser-verified the approved real Phase 10 language and Credits Settings
foundation.

Files affected: localization and credits services/routes, Settings markup/controller/KOKUEN styles,
shared time and Calendar week behavior, cache stamps, focused and rendered regressions, specifications,
Phase 10 status, task state, decisions, and wiki references.

Reason: replace typed regional fields with automatic custom controls, keep seven unreviewed languages
honestly unavailable, remove the conflicting Calendar week control, and expose a useful but explicitly
incomplete machine-audited Credits inventory.

Change: delivered and fully verified Afterlife Phase 10 localization and release hardening.

Files affected: eight local catalogs and manifest, localization runtime and Settings, localized
Task/Calendar parsers, translated READMEs, offline assets, complete credits/notices/licenses,
dependency and XML hardening, Aide health permissions, packaging, regressions, specifications,
architecture/trust records, and Phase 10 evidence/checklists.

Reason: ship an honest bounded eight-language product contract, exact native/container credit
artifacts, and separate current compatibility, accessibility, performance, security/privacy,
recovery, capability, architecture, regression, and visual proof without using owner data or making a
commit.

Change: closed the final mobile dependency and generated-license audit gaps.

Files affected: the mobile package/lock, Capacitor and release-packaging regressions, credits
generator, regenerated manifest/notices/licenses, Phase 10 security evidence, and delivery counts.

Reason: move Capacitor's vulnerable transitive tar line to fixed 7.5.20 and make refreshes remove stale
generated licenses so the audited and packaged notice bundle is exact.

Change: completed Afterlife Phase 11 review and final handoff.

Files affected: owner/Vault/Journal password policy and UI, HTML-mail privacy, Aide Full access copy,
Docker context exclusions, focused and browser regression tests, Phase 11/final-review records,
Afterlife indexes, and current task state.

Reason: reconcile all 41 original requests and 27 final-acceptance rows against the real implementation,
fix every confirmed in-scope blocker, prove macOS/Linux/Actual/recovery/browser behavior with isolated
data, and leave the two real original-request gaps explicit without autoreview, mockups, commits, or
pushes.

Date: 2026-07-21

Change: closed the targeted Aide token-mark defect and prepared exact owner-review starters for the two
remaining material product gaps.

Files affected: Aide top-bar usage markup/runtime/styles and focused regressions; Plan-board and
scheduled-News design review, standalone KOKUEN starters, shared local-only starter behavior, rendered
starter gate, and current task state.

Reason: make latest-response usage visible and accessible in the approved Aide shell, use the supplied
Kanban reference without duplicating its database or forbidden controls, and obtain required owner
approval before changing the real Plan or News implementation.

Date: 2026-07-22

Change: recorded owner approval of the exact Plan-board and scheduled-News KOKUEN starters.

Files affected: remaining-gaps design review and current task state.

Reason: clear the required material-interface approval gate before changing the real Plan or Aide
Scheduled implementation.

Date: 2026-07-22

Change: implemented the approved real Plan board and scheduled News workflow, corrected the incognito
reference mark, and reconciled the final system inventory and handoff evidence.

Files affected: existing Task reorder routes and Plan board UI; News database migration, service,
routes, scheduler, Home/Jarvis/Library integrations, Aide Scheduled UI, localization and cache
identity; incognito mark and module identities; focused backend, JavaScript, and rendered browser
tests; trust/data records, remaining-gap design review, final review, and task state.

Reason: close the two material post-Phase-11 product gaps without creating a second task store, make
the private digest durable and useful through source or model failure, match the approved incognito
reference in every state, and leave only the token glyph's explicit owner visual-approval boundary
unchecked. All implementation and verification used isolated data; no autoreview, stage, commit, or
push was performed.

Date: 2026-07-22

Change: fixed every confirmed blocker from the complete dev-afterlife autoreview before its clean rerun.

Files affected: native install and CLI safety; Actual bridge and migration staging; Files claims and root shares; public media network guard; Journal, Docs, Notes, localized input, Calendar, Plan, Projects, Home, and Aide runtime; Jarvis handoff and News delivery; Finance validation; design tokens and decision 0004; release documentation, credits generator, licenses, and focused regressions.

Reason: close real security, concurrency, data-preservation, routing, localization, schema, and redistribution-compliance defects found across all bounded review shards while preserving owner data and leaving the real worktree unstaged and uncommitted.

Date: 2026-07-22

Change: closed every confirmed finding from the latest protected and open autoreview shards and
strengthened exact regression coverage for centralized Actual-mode mutation blocking.

Files affected: browser passwords; Journal and Settings; native update CLI; Aide, Files, News,
Calendar, Andromeda, Docs, and scheduled News UI; Journal migration; file restore and offline cache;
vault transfer; S3 deletion; subscriptions; release planning records; and focused regressions.

Reason: prevent authority extension, timezone drift, host-path disclosure, unsafe concurrent update
commands, unapproved full-access turns, duplicate restore publications, symlink traversal, stale UI
state, partial-vault restoration, and partial strict S3 deletes while recording the reviewed Money
reports honestly as centrally guarded false positives.

Date: 2026-07-22

Change: closed the final aggregated autoreview blockers and prepared the clean rerun.

Files affected: Python dependency lock/install paths and generated notices; native update rollback;
Actual budget deletion; News run leasing; Discord outbox delivery; Mail, Calendar, Plan, localization,
Chat, Docs, Projects, subscriptions, backups, vault transfer, managed Actual, mutating browser gates,
and focused regressions.

Reason: make every confirmed first-pass finding safe, durable, reproducible, and test-covered while
preserving the real worktree as unstaged and uncommitted.

Date: 2026-07-22

Change: changed Alles's universal default server port from `8000` to `6769`.

Files affected: runtime and CLI defaults, Docker environment/exposure/healthcheck, Mail OAuth local
callback fallback, Settings compatibility URL, browser extension, Obsidian companion, Capacitor
scaffold, `.env.example`, English and translated READMEs, specifications, trust-map example, and
focused regression tests; shell stamp and service-worker cache identity.

Reason: keep every shipped local entry point and instruction aligned with the owner's selected port.
The `PORT` environment variable still overrides the default. An isolated live instance passed health
and Home checks on `127.0.0.1:6769` and delivered the refreshed offline shell; no owner data,
autoreview, stage, commit, or push was involved.

Date: 2026-07-22

Change: closed the additional blockers exposed by the final autoreview rerun.

Files affected: Jarvis outbox privacy filtering and tests; the Python 3.11 dependency lock and
regenerated credits; native update cleanup and regression coverage; Plan starter filter selection;
the KOKUEN partial-state browser fixture; and current task/decision records.

Reason: keep summary-only delivery data bounded, preserve the documented runtime floor, prevent a
rejected update from leaving maintenance locked, keep filtered task details honest, and make the
final browser/release gates exercise the current implementation.

Date: 2026-07-22

Change: closed every confirmed blocker from the complete final autoreview scope and prepared the
current snapshot for clean review and repository-wide verification.

Files affected: native update/install recovery; Files comments and offline cache; browser passwords;
Plan board ordering; setup paths and localization settings; dependency credits; Actual bridge; News
leasing; Obsidian companion installation; Aide starter accessibility; and focused regressions.

Reason: prevent orphaned recovery data, sealed-release mutation, cross-location replies, sliding
unlock authority, poisoned writes, nested private storage exposure, omitted nested notices, duplicate
Actual staging, ambiguous imports, expired pipeline ownership, rollback-copy loss, symlink following,
and unsupported Ollama reasoning controls while leaving the real worktree unstaged and uncommitted.

Date: 2026-07-22

Change: closed the last confirmed findings from the stale final-review shards before rebuilding the
current autoreview snapshot.

Files affected: Andromeda and Aide stream UI; vault transfer; setup and runtime access validation;
native release staging and per-release source metadata; focused JavaScript and Python regressions;
current task state.

Reason: preserve relative cross-app routes, keep reasoning ahead of its answer, serialize destructive
vault transitions, reject collectively unrestricted network trust, restore the exact prior Git source
on rollback, and prevent release staging from reading through descendant symlinks.

Date: 2026-07-22

Change: closed the confirmed blockers found while refreshing the complete autoreview snapshot.

Files affected: background Aide authorization and complexity classification; Actual restore;
Journal migration; Home localization; Obsidian companion installation; remote versions and storage
defaults; Discord pairing and handoff formatting; native ownership and uninstall recovery; vault
transfer serialization; S3 versioning behavior; Docs conflict handling; focused regressions and task
state.

Reason: prevent impossible detached approvals, missed CJK complexity, unresolved restore intents,
timezone drift, partial journal publication, companion rollback loss, unrecoverable remote versions,
storage races, Discord formatting and pairing abuse, orphaned native directories, interrupted
uninstall corruption, concurrent vault mutation, false S3 version claims, and stale Docs conflict
comparisons while keeping the real worktree unstaged and uncommitted.

Date: 2026-07-22

Change: closed the confirmed findings from the rebuilt five-shard autoreview and added interruption,
concurrency, localization, platform, responsive-layout, and stale-session regressions.

Files affected: native update CLI; document drafts; Journal migration; remote Files restore; News
leasing; Andromeda, model selection, Plan, Home, and Docs UI; managed Actual and native install
platform behavior; Afterlife status documentation; focused Python and JavaScript tests.

Reason: keep maintenance authority through startup, publish recovery state atomically, resume every
durable migration transition, reject unstable remote overwrites, prevent stale workers and sessions
from changing current state, honor configured locale/platform boundaries, and keep the current
handoff honest without staging, committing, or pushing the real worktree.

Date: 2026-07-22

Change: closed every confirmed blocker from the last stale autoreview shards and added exact
regressions for their concurrency, recovery, authority, timestamp, and localization boundaries.

Files affected: browser extension pairing; subscriptions; native install/update; vault transfer;
scheduled News; Docs draft safety; canonical Finance import recovery; local share revocation;
Journal migration; Files-to-Photos provenance; Plan starter behavior; automatic currency mapping;
focused Python and JavaScript tests; and current task state.

Reason: keep transient pairing and renewal failures isolated, make destructive publication and native
recovery restart-safe, serialize every News and update command path, prevent stale draft loss, retain
undo ownership for owner-accepted Actual replacements, remove incomplete plaintext staging, preserve
UTC source identity, and derive automatic currencies from the repository-pinned CLDR data without a
false USD default. The protected OAuth rename and centrally guarded legacy Money reports were
recorded as review artifacts rather than changed in production.

Date: 2026-07-22

Change: closed the remaining confirmed blockers from the refreshed full-scope autoreview and added
focused durability, concurrency, recovery, protocol, localization, and state-preservation tests.

Files affected: native install/update; terminal handoff; browser unlock; backup/restore; S3 and
WebDAV storage; Discord; Plan; offline cache; Andromeda; FX fixtures; document and Journal safety;
agent effort/background policy; localized input; Home Settings; focused Python and JavaScript tests;
Notes frontmatter; native-update test contracts; and current task state.

Reason: prevent partial or stale state from being accepted across destructive and asynchronous paths,
keep safe storage/version boundaries fail-closed, preserve custom owner configuration, and ensure the
clean autoreview rerun describes the exact current worktree without staging, committing, or pushing it.

Date: 2026-07-23

Change: closed every confirmed blocker from the latest five-shard current-snapshot autoreview and
added exact regressions for encoding, durability, identity, recovery, authority, and full-vault scan
boundaries.

Files affected: CardDAV and guarded HTTP handling; Journal migration; Files restore; News URL
normalization; Finance imports; isolated browser gates; Actual budgets; Docs drafts; browser password
matching; Discord outbox and gateway recovery; focused Python and JavaScript tests; current task and
decision records.

Reason: preserve source bytes and requested destinations, make filesystem publication crash-durable,
prevent unrelated statement and deletion identities from colliding, keep large editor drafts
recoverable across immediate departure, find valid credentials beyond an arbitrary query prefix, and
stop disabled or quiet Discord connectors at the last network boundary while retaining only a safe
resumable checkpoint.

Date: 2026-07-23

Change: closed the confirmed blockers from the refreshed current-snapshot shards and added focused
regressions for filesystem identity, asynchronous state ordering, editor durability, service health,
calendar placement, media cleanup, and upload identity.

Files affected: Notes, Finance, Shell, Read, native update CLI, Actual bridge, Calendar, Voice, Files,
Journal, file-operation path claims, Uploads, focused Python and JavaScript tests, and task state.

Reason: preserve owner-reviewed data and unsaved drafts, bound disconnect cleanup, serialize durable
saves, target the owned native service regardless of caller environment, recover remote staging,
prevent stale UI responses and DST normalization, release failed media resources, collide local paths
according to filesystem semantics, and keep upload identifiers canonical and unique across stores.

Date: 2026-07-23

Change: closed the confirmed backup ownership/concurrency, Actual transfer balance, empty WebDAV,
native gate ownership, hidden share path, and Discord restart findings from the final autoreview.

Files affected: automatic backup; Actual migration; WebDAV storage; native current-host gate; public
folder shares; Discord delivery; focused Python tests; and current task state.

Reason: prevent one installation from pruning another's backups, stop duplicate daily runs and
unbalanced transfers, permit safely proven first writes to empty WebDAV collections, keep destructive
gates inside separately owned roots, hide dot-path content consistently, and edit the original
Discord stream after restart.

Date: 2026-07-23

Change: closed the remaining confirmed findings from the final open-scope autoreview and added exact
regressions for bounded bridge output, stable recurrence, native endpoint reporting, permission-safe
microphone fallback, background intent, Home capture feedback, imported payee clearing, cancellation
retention, unsupported offline copies, and explicit Andromeda fallbacks.

Files affected: Actual bridge and canonical Finance integration; native install CLI; Aide background
policy; Voice and Home UI; Uploads; offline Files; Andromeda; focused Python and JavaScript tests; and
current task state.

Reason: prevent unbounded child output, rejected schedules, misleading setup URLs, repeated permission
prompts, missed explicit background work, hidden capture success, stale imported identities, unbounded
cancellation state, platform crashes, and invisible fallback persistence while preserving the pinned
Actual 26.7.0 `addTransactions` contract.

Date: 2026-07-23

Change: closed the final protected and open-scope autoreview findings and added focused regressions for
claim release, local filesystem aliases, browser unlock expiry, restart-safe Discord streams, Actual
opening balances, native release recovery, offline Windows namespaces, calendar wall dates and week
starts, localized owner text, bounded voice transcription, upload tombstones, Docs recovery choices,
and starter keyboard/modal accessibility.

Files affected: Files claims and offline cache; browser extension; Discord; Actual migration and
canonical metadata; native install; Calendar and date picker; localized quick add; Voice; Uploads;
Docs; specialist and navigation starters; bank-import planning; focused Python and JavaScript tests;
and current task state.

Reason: release inactive ownership, prevent cross-platform cache collisions and stale asynchronous
state, preserve every recoverable owner copy, keep locale and wall-date behavior consistent, make
native cleanup resumable, and align planning documentation with the actual stable-identity contract.
The pinned Actual result contract and protected passkey rename were retained as review artifacts.

Date: 2026-07-23

Change: closed the remaining confirmed findings from the final current-snapshot autoreview and
updated stale regressions to exercise the hardened production contracts.

Files affected: local file claims; S3 storage; Discord; Uploads; News; subscriptions and Finance;
native install; Aide and Andromeda handoff; localized input and currency; Home, Calendar, and Voice;
Read, Vault, remote Files, and PTY tests; and current task state.

Reason: canonicalize local filesystem ownership, reject unsafe conditional writes, stop network work
after live authority changes, preserve durable cancellation and stream recovery, prevent stale UI
responses, keep locale behavior accurate, require exact money values, and make repository discovery
tests independent of optional browser tooling.

Date: 2026-07-23

Change: closed the final confirmed current-snapshot autoreview blockers and added focused regressions
for native health ownership, transfer rollback, draft concurrency, timezone ordering, active XML,
upload cancellation, offline integrity, and Journal navigation.

Files affected: native update CLI; Actual bridge; document safety and Docs; Plan and Journal; Files,
offline cache, and Uploads; credits generation and notices; focused Python and JavaScript tests; and
current task state.

Reason: reject redirected or unowned health responses, avoid double-deleting reciprocal transfers,
preserve new drafts and edits across asynchronous work, normalize mixed timestamp ordering, keep XML
inert, retain cancellation tombstones, verify every offline file and failed removal, and record the
authoritative socksio license.

Date: 2026-07-23

Change: closed every confirmed finding from the newest five-shard autoreview and added focused
regressions for authority rechecks, durable delivery and cancellation state, filesystem identity,
safe remote creation, native service ownership, release cleanup, session state, and bounded replay.

Files affected: Discord and Days delivery; Files and offline storage; Uploads; Docs; News; localized
input and date picker; Actual migration; Journal migration; Cookbook; native install; WebDAV;
model selection; service worker; isolated browser gates; focused Python and JavaScript tests; and
current task state.

Reason: prevent stale authority and stale UI state, preserve reviewed owner data, keep destructive
operations restart-safe, respect real filesystem and remote-store semantics, avoid touching
preexisting services, and fail closed without hiding independent verification errors.

Date: 2026-07-23

Change: closed every confirmed blocker from the completed five-shard autoreview, added focused
regressions, and verified the owner's lower-cost autoreview defaults.

Files affected: native update and uninstall recovery; Actual bridge and migration; Finance import
identity and specialist controls; local claim migration; Docs and Journal recovery; WebDAV transport;
model selection; focused Python and JavaScript tests; and current task state.

Reason: preserve service and release authority across crashes, prevent wrong-budget deletion and
recurrence drift, keep repeated bank rows and UI mutations deterministic, respect mounted filesystem
case behavior, retain recoverable editor writes, pin WebDAV connections to validated DNS results, and
keep saved session objects consistent after navigation. Terra/medium and a two-pass maximum are now
confirmed effective and require coherent review splits for oversized deliveries.

Date: 2026-07-25

Change: implemented the approved Phase 12 product consolidation and corrected the final rendered
navigation, mobile layout, and Home customization defects found during the capstone audit.

Files affected: Phase 12 workbench routing and markup; KOKUEN runtime CSS; Andromeda answer and
verification UI/services; Server policy and management workbench; Home and Settings; compatibility,
cache, source, Python, JavaScript, and Playwright tests; specifications; and project knowledge.

Reason: expose one honest nine-workbench product, keep cited search fast without conflating it with
independent verification, make Server useful without granting arbitrary host control, preserve the
existing neofetch/btop implementation, and require fresh rendered evidence before handoff.

Date: 2026-07-26

Change: implemented typed Aide app operations, SimpleFIN/Plaid bank connections, safe external Obsidian
vault locations, and pinned AdGuard Home and Nginx Proxy Manager companions with native Server surfaces.

Files affected: registry and generated catalogs; Aide tools; Finance models, migration, connectors,
routes, UI, and tests; Files vault transfer routes/UI/tests; managed companion lifecycle, encrypted
clients, Server routes/UI/tests; credential inventory; Afterlife decisions; and task state.

Reason: make the planned capabilities real without scraping provider sessions, exposing secrets to
models, claiming external services, silently changing network configuration, or treating local tests as
a live-network pass.

Date: 2026-07-27

Change: added an executable, benchmark-informed model and reasoning-effort route for every unfinished
delivery and real-computer acceptance task.

Files affected: task-routing registry and validator; generated execution plan; focused routing tests;
and current task state.

Reason: use Terra for ordinary execution and deterministic proof, reserve Sol and deeper reasoning for
quality-critical, security, recovery, and destructive work, and prevent unavailable models, unrouted
scenarios, or max-effort defaults from silently entering the completion program.
Date: 2026-07-30

Change: migrated all nine specialist workbenches to KOKUEN v5 and closed the degraded-load,
recent-owner, destructive-action, semantic-row, tab, focus, and failed-persistence gaps found by the
real application audit.

Files affected: specialist shell markup and routing; shared KOKUEN choices and tabs; Docs, Files,
Books, Read, Health, Finance, Vault, and Server browser modules and styling; focused JavaScript,
Python, and Playwright gates; and current task state.

Reason: keep one universal workbench grammar without hiding owner data, mistaking request failures for
empty collections, duplicating privileged writes, exposing pointer-only controls, or leaving rejected
choices visible.

Date: 2026-07-31

Change: expanded the post-v5 literal interaction census and repaired defects exposed by populated,
repeated real-application use.

Files affected: control census generator, overrides, JSON and readable map; shared KOKUEN styling and
state reflection; Plan task controls and dialog; specialist routing; rich-demo Finance provenance;
localization; focused JavaScript, Python, and Playwright gates; and current task state.

Reason: reconcile every discoverable control path without overclaiming tests, then make populated Plan,
Calendar, Inbox, Finance, Vault, and Server behavior meet the same target, focus, repeat, error, and
canonical-shell contract as their empty resting surfaces.

Date: 2026-07-31

Change: rebuilt and exercised the real Aide model-selection and effort path against an isolated local
OpenAI-compatible provider boundary.

Files affected: Aide model dialog markup, model picker and shell behavior, KOKUEN overlay, model-selection
regressions, real model/effort browser gate, generated control census, and current task state.

Reason: replace a pointer-only model list and incomplete modal semantics with a keyboard-complete focus
boundary, then prove every effort, repeated selection, streaming, compatibility failure, automatic retry,
manual recovery, and persisted reload without using owner credentials or paid provider quota.

Date: 2026-07-31

Change: completed the repeated literal control scan, audited every canonical KOKUEN advisory, and
exercised Chrome-owned extension permission/pairing and standalone PWA installation through Computer Use.

Files affected: canonical lint audit script, test, and documentation; current task and decision ledgers;
real-app control evidence; owned Chrome testing profile and temporary application data only.

Reason: make zero unexplained warnings a deterministic gate, distinguish automated web proof from native
Chrome acceptance, and verify the universal shell in the actual installed delivery surface without using
owner data or leaving the isolated PWA installed.

Date: 2026-07-31

Change: normalized every shipped inline product-text declaration below 12px to the KOKUEN hard floor and
added a regression scan for static and dynamically rendered templates.

Files affected: main browser markup; compact model, settings, calendar, contacts, Files, Photos, Vault,
reminder, comparison, project, and application-settings renderers; KOKUEN lint regression and audit.

Reason: the canonical overlay linter cannot see inline template styles, and the final point-by-point audit
found legacy metadata and action labels that rendered below the non-negotiable 12px text floor.

Date: 2026-07-31

Change: closed the final real-use races exposed by repeated Docs, extension, schedule-send, and model-picker
review, then reran the literal control census after the complete source corrections.

Files affected: Notes editor refresh ownership; extension popup lock and stale-result ownership; Aide
send-later busy/error behavior; model option button reset; focused JavaScript and Playwright evidence;
literal census, generated logic map, and current task state.

Reason: prevent background refresh from deleting live edits, prevent stale extension work from repainting
or filling after lock, reject duplicate scheduled messages while retaining a useful retry surface, and
ensure semantic option buttons never fall back to browser chrome or a sub-44px hit area.

Date: 2026-07-31

Change: completed the final KOKUEN v5 logic-map, automated, browser, Computer Use, visual, lint, and
independent-regression acceptance program.

Files affected: deterministic 1,612-control and 2,730-path census; KOKUEN warning audit; final real-app
gates and captures; exhaustive Python and JavaScript suites; project task, decision, and change ledgers.

Reason: finish from current rendered evidence rather than source confidence, preserve explicit external
acceptance boundaries, and leave a reproducible local proof set before the no-push checkpoint commit.
