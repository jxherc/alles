fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
python-dotenv==1.2.2
httpx==0.28.1
defusedxml==0.7.1          # entity-safe parsing for remote XML feeds and DAV/S3 responses
pydantic==2.13.4
sqlalchemy==2.0.51
cryptography==49.0.0
bcrypt==5.0.0                # password hashing (core/auth.py)
fastembed==0.8.0
python-docx==1.2.0          # vault .docx export
pillow==12.3.0               # photos app — thumbnails + EXIF
pillow-heif==1.4.0; sys_platform == "darwin"  # photos — HEIC/HEIF decode + thumbnails on macOS
numpy==2.4.6                # photos — justified layout math, places reverse-geocode, clip vectors
beautifulsoup4==4.15.0       # research — html parsing
trafilatura==2.1.0          # research — main-article extraction (strips nav/ads/boilerplate)
ddgs==9.14.4                 # research — keyless web search (formerly duckduckgo-search)
psutil==7.2.2               # system monitor — live cpu/ram/disk (degrades gracefully without it)
# optional — computer use tools (screenshot/click/type). install if you enable it.
# pyautogui
# optional — offline voice input (local whisper STT). install if you enable it.
# faster-whisper
# optional — calendar CalDAV sync (iCloud/Google). install if you enable it.
# caldav
# optional — CLIP semantic search (photos "smart" search). install these, then drop the
# immich-app/ViT-B-32__openai ONNX models into models/clip/ (visual/, textual/) to enable.
# onnxruntime
# tokenizers
# optional — face recognition (photos "people"). install insightface (pulls in opencv/scipy/
# scikit-image); the buffalo_l models auto-download into models/faces/ on first run.
# insightface
# onnxruntime
